# spec_omnetpp_event_kernel

Models `520.omnetpp_r` style discrete-event simulation with priority queue
updates, pointer-like indexing, and branch-heavy event dispatch. This is a
representative RTL kernel, not SPEC source.
