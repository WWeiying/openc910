# 玄铁 C910 处理器体系结构总览

> 本文以经典体系结构教材的框架（Hennessy & Patterson《计算机体系结构：量化研究方法》、
> Shen & Lipasti《现代处理器设计》、Smith & Sohi《超标量处理器微架构》）为骨架，
> 串联 C910 的结构参数、数据流和控制边界。RTL 可以证明“实现了什么、何时更新、怎样连接”；
> 对设计动机、PPA 和性能收益的教学解释会与可验证事实分开表述，并以综合或实测结果为最终依据。
>
> 配套框图：`C910_架构框图.png` / `.svg`（graphviz 渲染，源文件 `C910_架构框图.dot`）。
> 各单元的行级源码细节见 [IFU](./ifu/README.md)、[IDU](./idu/README.md)、
> [IU](./iu/README.md)、[LSU](./lsu/README.md) 和 [RTU](./rtu/README.md)。

---

## 目录

1. [一句话定位与设计目标](#1-一句话定位与设计目标)
2. [ISA 与编程模型](#2-isa-与编程模型)
3. [总体流水线：12 级乱序超标量](#3-总体流水线12-级乱序超标量)
4. [取指前端 IFU 与分支预测](#4-取指前端-ifu-与分支预测)
   - 4.4 取指三级流水 IF / IP / IB（控制 + 数据通路）
5. [乱序引擎 IDU：译码 / 重命名 / 发射](#5-乱序引擎-idu译码--重命名--发射)
6. [执行单元 IU / VFPU](#6-执行单元-iu--vfpu)
   - 6.3 VFPU 详细架构（pipe6/pipe7 子单元、延迟、不对称设计）
   - 6.5 指令执行延迟速查表（整数/访存/浮点全覆盖）
   - 6.6 产品向量架构与当前开放 RTL 边界
7. [访存子系统 LSU](#7-访存子系统-lsu)
   - 7.5 内存顺序模型（RVWMO / fence / sfence.vma / AMO-LR-SC）
8. [存储层次：Cache 与 MMU](#8-存储层次cache-与-mmu)
   - 8.2 L1 I-Cache 内部（tag/predecode/way-predict/per-bank 门控）
   - 8.3 L1 D-Cache 内部（双 tag/8 bank/dirty 阵列/cache_buffer/仲裁）
   - 8.4 L2 Cache 内部（2 sub-bank/tag-cmp-data-wb/可编程延迟/ECC 框架/预取）
   - 8.5 MMU 内部（uTLB-PLRU/JTLB one-hot 轮转/ASID/huge 页/PTW/XMAE/sysmap）
9. [退休与精确异常 RTU](#9-退休与精确异常-rtu)
   - 9.5 流水线 Stall 条件系统化（IR stall / IS dispatch stall / 访存 stall）
10. [多核与缓存一致性](#10-多核与缓存一致性)
    - 10.2 产品 MOESI 口径与当前 RTL 可证明的状态边界
    - 10.3 CIU 架构与 SNB 侦听流程
    - 10.4 AXI ACE 请求类型
    - 10.5 多核常见性能陷阱
    - 10.6 BIU：每核 AXI/ACE 主端口
11. [中断与异常处理（trap / CLINT / PLIC）](#11-中断与异常处理trap--clint--plic)
    - 11.1 统一陷入入口 · 11.2 CLINT · 11.3 PLIC · 11.4 取舍小结 · 11.5 复位与启动
12. [低功耗与时钟门控](#12-低功耗与时钟门控)
    - 12.1 三级时钟门控 · 12.2 省功耗手法汇总 · 12.3 WFI 低功耗模式
13. [物理内存保护 PMP 与 可观测/调试（PMU / HAD）](#13-物理内存保护-pmp-与-可观测调试pmu--had)
    - 13.1 PMP · 13.2 PMU/HPCP · 13.3 HAD 调试与跟踪 · 13.4 DFT 可测试性
14. [端到端数据流走查（把所有单元串起来）](#14-端到端数据流走查把所有单元串起来)
    - 走查 ALU / load miss / 分支误预测 / 跨核一致性 四条全程
15. [核心体系结构权衡总表（对照教科书）](#15-核心体系结构权衡总表对照教科书)
16. [与经典处理器的横向对比](#16-与经典处理器的横向对比)
17. [全参数速查表](#17-全参数速查表)
18. [术语与缩略语速查表](#18-术语与缩略语速查表)

---

## 1. 一句话定位与设计目标

**C910 是一颗 RV64GC 指令集、12 级流水、3 路译码 / 8 条执行管线的乱序超标量
（out-of-order superscalar）高性能应用级处理器**，对标 ARM Cortex-A72/A73 量级，
目标是跑得动 Linux、追求高 IPC 与高频率的折中。

设计上贯穿三条主线，理解它们就理解了 C910 的全部取舍：

| 主线 | 含义 | 教科书对应 |
|------|------|-----------|
| **挖掘 ILP** | 乱序执行 + 寄存器重命名 + 推测执行，让无关指令并行跑 | H&P 第 3 章「指令级并行」 |
| **隐藏访存延迟** | 多级 cache + 8 项 RB/LFB 地址跟踪 + 硬件预取 + store 缓冲 | H&P 第 2 章「存储层次」 |
| **保证正确性** | 顺序退休（ROB）+ 精确异常 + 缓存一致性 + 访存顺序违例检测 | Smith&Sohi「精确中断」 |

作为商用核，C910 在窗口容量、端口数量、状态保存和流水边界之间做了大量取舍。RTL
能够显示资源复用和门控意图，但“节省多少面积、功耗或时序”仍要由目标工艺下的实现报告量化。

---

## 2. ISA 与编程模型

| 项目 | C910 |
|------|------|
| 基础 ISA | RV64GC = RV64 **I**MAFD**C**（64 位整数 + 乘除 + 原子 + 单/双精度浮点 + 压缩指令） |
| 自定义扩展 | **XIE**（XuanTie 指令扩展，即 `xtheadc`：addsl/ext/extu/ff1/rev/mveqz/tstnbz 等）、**XMAE**（内存属性扩展，给页表项加属性位） |
| 向量 | 产品论文描述早期 Vector 0.7.1；当前开放 RTL 的 `misa.V=0`，向量译码、VRT 与向量 PST 含关闭或 dummy 路径，不能按完整 RVV 核运行 |
| 特权级 | M / S / U 三级，特权规范 v1.10，可跑完整 Linux |
| 虚拟内存 | Sv39（39 位虚地址，三级页表） |
| 兼容性 | 应按具体产品版本、开放 RTL 配置和对应测试报告分别判断；RTL 本身不能证明某套兼容性认证已经通过 |
| 浮点 | IEEE 754-2008，**不产生浮点异常**（用 fflags 标志位，不 trap）——简化了精确异常逻辑 |

**为什么自定义 XIE 扩展值得单独测量？** RISC-V 基础指令集没有 `addsl`、`ext`、
`ff1` 等玄铁复合操作，编译器启用 XTheadC 后可以减少部分热点的动态指令数。不过本仓库
历史 CoreMark 的约 4.4 与 6.1 分数来自不同构建配置，编译选项、代码布局和运行参数也会
同时变化，不能把全部差值直接归因于 ISA。可靠方法是只改变 `-march`/扩展开关，固定其余
二进制生成和仿真条件，再比较动态指令、周期数与指令类别。

**"G"和"C"各自的分量，以及 C 的隐藏代价**：G = IMAFD（整数 + 乘除 + 原子 + 单/双精度浮点），
是运行通用 OS/应用的基础；**C（压缩指令）**用 16/32 位混合编码，通常能降低代码体积并改善
取指带宽利用率，幅度取决于工作负载和编译器。但 **C 扩展不是免费的**——它让指令边界变成**动态的**（一条指令可能
16 或 32 位、甚至跨 cache 行），逼着前端做预解码（§4.3 的 predecode 阵列、§4.4 的 BRY 边界位）
来提前算边界。**这是一处典型的"ISA 决策溢出到微架构"**：指令集为省代码做的选择，前端就得
多花硬件来消化——体系结构的取舍常常跨层传导，读这份文档时要时刻留意这种因果链。

**"浮点不 trap、只置 fflags"为什么是一笔好买卖**：IEEE 异常（除零/上溢/无效…）在 C910 里
不产生陷入，只在 `fflags` 累加标志位，软件想知道再去读。**对乱序核这是一大简化**：若浮点会
trap，乱序窗口里每条在飞的浮点指令都可能在中途"诈"出异常，精确异常逻辑（§9.3）就得为它们
额外保留可回滚现场；改成"不 trap 只置位"后，浮点指令和普通运算一样"不会失败"，§6.4 的
"推定完成"才能成立。**用 ISA 层一个约定，换后端精确异常逻辑的大幅简化**——又一个跨层取舍。

**模块化 ISA 的思想**：RISC-V 是"小而稳的基座（RV64I）+ 可选扩展（MAFDC…）"的搭积木式
指令集。C910 在标准之上叠加私有的 XIE/XMAE，正是吃到了这种可扩展性的红利——既与标准工具链
兼容，又能针对自己的热点加专用指令。这与 x86/ARM"大一统巨型 ISA"是两条不同路线（H&P 第 1 章
ISA 设计原则）。

---

## 3. 总体流水线：12 级乱序超标量

### 3.1 流水级总览

C910 整数主流水线官方称 **12 级**（向量/浮点更长）。按功能可分为**顺序前端**、
**乱序执行**、**顺序退休**三段——这正是教科书里超标量乱序核的标准三段式
（Shen & Lipasti 称为 in-order front-end / out-of-order core / in-order retirement）：

```
┌──────────────── 顺序前端 (in-order) ────────────────┐┌─── 乱序 (OoO) ───┐┌─ 顺序退休 ─┐
                                                                                          
  PCGEN → IF → IP → IB →│ ID → IR → IS →│ RF →│ EX1 EX2 ...    →│   退休/提交
  ───────IFU───────  IBUF  ──────IDU──────      ───执行单元───      ────RTU────
  取指/分支预测      缓冲   译码/重命名/发射  读寄存器  ALU/LSU/FPU      ROB 按序退休

  ◄──────────────────── 顺序 ─────────────────►◄─── 乱序窗口 ───►◄── 顺序 ──►
```

| 阶段 | 流水级 | 单元 | 关键动作 |
|------|--------|------|----------|
| 取指 | PCGEN / IF / IP / IB | IFU | PC 生成、I-Cache 访问、预测、预解码、打包进 IBUF |
| 译码 | ID | IDU | 3 路并行译码、复杂指令拆分 |
| 重命名 | IR | IDU | 架构寄存器→物理寄存器，消除假依赖 |
| 发射 | IS | IDU | 分发到 7 个发射队列，唤醒-选择乱序发射 |
| 读寄存器 | RF | IDU | 读 PRF + 前递网络旁路 |
| 执行 | EX1~EX5 | IU/LSU/VFPU | 各功能单元，延迟不一 |
| 退休 | — | RTU | ROB 按程序序提交，精确异常 |

### 3.2 关键宽度参数（决定 IPC 上限）

| 维度 | 宽度 | 说明 |
|------|------|------|
| 取指宽度 | 128 bit / 拍，即 16 字节 | 每次处理一条 64B I-Cache line 的四分之一；窗口可容纳 8 个 16 位 half-word 或 4 条完整 32 位指令 |
| 译码宽度 | **3 条 / 拍** | 3 个并行译码器 |
| 派遣宽度 | **4 微操作 / 拍** | 拆分后可多于译码数 |
| 发射端口 | **8 条执行管线** | pipe0~7 |
| 退休宽度 | **3 项 / 拍**（折叠后峰值 9 条 / 拍） | ROB 退休口 |

**为什么"译码 3、发射 8、退休 3"不对称？** 这是经典的"漏斗"设计（H&P 称 issue
是超标量瓶颈）：执行端口多（8）是为了吸收瞬时爆发和不同类型指令并行；译码/退休窄
（3）是因为它们是顺序段，加宽的时序与面积代价陡增、收益递减。平均 IPC 受限于最窄
的顺序段≈3，但乱序窗口让突发并行度短时冲到 8，整体逼近 3 这个上限。

**为什么是 12 级，不是更深或更浅？** 这是另一道经典权衡——**流水深度**（H&P 第 3 章
「流水线深度的代价」）。把同样的工作切成更多级，每级逻辑更少→**时钟更快**；但更深要付三笔账：
- **误预测惩罚线性增大**：分支预测错时要冲刷在途指令，流水越深冲得越多（§4.1 估过 12 级
  一次误预测冲约 5~8 条）；Pentium 4 那种 20+ 级就是被误预测惩罚拖累的反面教材。
- **相关指令的转发/唤醒更难**：生产者与消费者隔得越远，背靠背依赖越难做到 1 拍（§6.2 的
  EX1 前递正是为对抗这点）。
- **锁存器面积/功耗增加**：每多一级就多一排触发器。

更浅（如经典 5 级 RISC）则反过来：误预测便宜、转发简单，但每级塞太多逻辑、频率上不去。
官方产品采用 12 级整数流水，是频率、单级逻辑深度、旁路距离和错误恢复代价之间的工程
折中。流水越深，越容易把复杂逻辑拆开并争取频率，但控制流错误从发现到重新形成有效供给
的距离也可能变长，所以产品同时配置了分级目标预测、Bi-Mode 方向预测和恢复路径。RTL
能够证明这些结构和寄存边界，不能单凭“12 级”断言它一定是某工艺下的最优甜点，也不能
把实际误预测代价固定成一个拍数；后者还受分支发现级、stall、reissue、重定向优先级和
分支到退休位置影响。

---

## 4. 取指前端 IFU 与分支预测

### 4.1 为什么前端这么重视分支预测

乱序核必须在分支真正执行以前持续取指，否则每遇到控制流都要等待寄存器就绪、执行和结果
返回，前端供给会频繁断流。误预测损失也不能只按流水级数估算：错误路径中已经取入、译码、
重命名甚至执行的工作都会被取消，随后还要等待正确目标重新通过 I-Cache、IBUF 和译码端。
C910 因而同时预测方向与目标，并把热点目标、普通目标、返回和间接跳转分开处理。预测器的
价值应由误预测次数、每次恢复空窗和恢复期间丢失的退休工作共同量化，而不是由结构数量推断。

### 4.2 分级分支预测器（精度 vs 延迟的权衡）

| 预测器 | 容量 | 延迟 | 负责 | 设计动机 |
|--------|------|------|------|----------|
| **L0-BTB** | 16 项寄存器阵列 | 组合比较后锁存命中，早于主 BTB 使用 | 热点跳转目标 | 绕开主 BTB 的同步 SRAM 路径；仍有 `entry_hit_flop` 和 IFCTRL 资格边界，不是 PCGEN 同拍改流 |
| **BTB** | 当前生成 RTL 为 512 行 × 4 个固定位置槽 | 同步 SRAM 流水 | 普通跳转目标与 I-Cache way 预测 | 四槽由取指块内位置决定，不是四路组相联；逻辑 index[9] 在当前 512 深度宏上未参与寻址 |
| **BHT** | 64 Kbit Predict Array + 2 Kbit Select Array，**Bi-Mode** | 分级流水 | 条件分支方向 | Taken/Not-Taken 两个逻辑侧减少相反偏置的直接干扰，但仍存在同侧和索引别名 |
| **RAS** | 12 项投机栈 + 6 项退休恢复副本 | 组合选择与流水反馈 | 函数返回地址 | 后 6 项用于精确恢复，不是额外预测深度；准确率取决于调用模式、溢出和非标准返回 |
| **Loop Buffer** | 16 项循环缓冲 | 专用循环路径 | 小循环 | 命中并激活后可由 LBUF 供给循环体，减少正常 I-Cache 取指活动 |

**两个最值得记的设计：**

1. **Bi-Mode BHT（方向预测）**：经典 2-bit 饱和计数器存在索引别名。C910 用 Select
   Array 在同一 64-bit Predict Array 行内选择 Taken 或 Not-Taken 逻辑侧，使相反偏置
   分支不必直接训练同一个计数器；同一侧和 Select Array 自身仍可能别名。22 位 GHR
   以分段选择和异或形式参与 10 位索引，不应简化成每次都把全部 22 位均匀折叠。

2. **投机 GHR + 退休 GHR 双历史**：取指时用投机更新的 VGHR（可能错）；RTU 退休时用
   精确的 RTUGHR。预测错时 `VGHR ← RTUGHR` 一键恢复。**以精度换吞吐**的典型手法。

### 4.3 取指数据通路要点

- **I-Cache**：64KB / 2 路 / 64B 行 / **VIPT** / 每组 1 bit 两路轮转。VIPT
  让虚拟 index 查询与 MMU 翻译并行，物理 tag 到达后再确认命中。替换位写使能为
  `{!fifo_bit, fifo_bit}` 并在 refill 后翻转，具有 FIFO 式轮转效果；它不维护命中
  次序，面积和命中率收益仍需由综合及 workload 验证。
- **Predecode 阵列**：refill 时顺手预解码（标出指令边界、分支类型），存进 cache。
  因为 RVC 压缩指令让指令边界是动态的（16/32 位混合），预解码避免 IP 级重复算边界。
- **Way Predict + Bank 精确激活**：预测访问哪一路、哪个 bank，只唤醒目标 SRAM，
  分别省约 50% / 75% 的 Data 阵列读功耗。
- **IBUF（32 项，每项一个 16 位 half-word）**：解耦取指与译码的弹性缓冲（`ENTRY_NUM=32`；
  因 RVC 让指令边界按 16 位对齐，故按 half-word 而非整指令组织，向译码每拍排出最多 3 条指令）；
  空且下游不堵时支持
  **bypass** 直通 IDU 省一拍。

### 4.4 取指三级流水 IF / IP / IB（控制 + 数据通路）

§4.2 讲"预测谁"，这里讲"取指三级怎么走"。C910 把取指拆成 **IF→IP→IB** 三级，每级一对
**控制(ctrl) + 数据通路(dp)** 模块，把 SRAM 访问、解码/预测、分支解析三件事流水开，
持续向后级形成 128-bit 取指窗口，并在边界、分支与缓冲允许时向 IDU 提供最多
3 条架构指令：

| 级 | 控制 ctrl | 数据通路 dp |
|----|-----------|-------------|
| **IF** | stall 汇聚（refill / way-pred 失败 / 预测器无效化）；`fence.i` 的 I-Cache 无效化状态机 | 4 段 tag 比较定命中/路；切出 **BRY 指令边界位**；打包 L0/L1 BTB 数据 |
| **IP** | **分支碰撞检测**（一组里多个分支则 stall）；BHT 读写冲突管理；BTB/BHT/RAS 目标仲裁；生成 **chgflw** | Way0/Way1 并行解码 H1~H8 半字（分支类型/偏移）；BHT 方向与 BTB 目标**融合**；H0 跨行残留缓冲 |
| **IB** | IBUF/LBUF 仲裁；**RAS push(call)/pop(ret)** 门控；间接跳转 BTB 状态机 | 算 RAS 压栈 PC 与间接目标；after-head 重排 / after-tail 截断；选 bypass/merge/写缓冲 |

**关键机制**：
- **BRY 半字对齐**：RVC 让指令可能从半字中间起；两套 bry 位（假设 H1 在低半 / 在新起点）由 ipctrl 按 H0 有效性选择。
- **chgflw 分层**：IP 级预测 taken 即拉高转向；IB 级再叠加更晚才知道的转向，优先级 **LBUF 回边 > RAS-pop > 间接 BTB > L0-BTB 纠正**。
- **RAS push/pop 仅在 IB 不 stall 时进行**——防止误预测恢复期破坏返回栈。
- **投机/精确**：GHR、vtype 在 IP 级投机更新，误预测由 RTU 恢复（呼应 §4.2 双历史）。

**为什么拆 3 级**：单级取指要一拍内做完 SRAM 读 + tag 比较 + 多分支解码 + 预测融合 + 缓冲，
关键路径太长收不了高频；拆三级各司其职、用流水深度换频率（代价是误预测多冲几级，§6.2 用
前端早重定向补偿）。

> 细节见 [IFU 文档索引](./ifu/README.md)（00 至 20 共 21 篇）与
> [分支预测专题](./branch_prediction.md)。

---

## 5. 乱序引擎 IDU：译码 / 重命名 / 发射

IDU 是全核最大单元（56 个 RTL 文件，约 10.4 万行），它是**顺序→乱序的转换点**。

### 5.1 IR 重命名：消除假依赖（乱序的前提）

乱序执行必须先解决三类数据冒险中的两类"假依赖"：

```
RAW（真依赖）：写后读 —— 无法消除，靠唤醒+前递加速
WAR（假依赖）：读后写 ┐
WAW（假依赖）：写后写 ┘ 源于"架构寄存器只有 32 个"被反复复用 → 重命名消除
```

**寄存器重命名原理**：给每次写入分配一个全新的物理寄存器（physical register），
架构寄存器 x0-x31 只是"别名"。重命名表 RAT 记录"每个架构寄存器当前映射到哪个 preg"。
这样两条写同一个 x1 的指令用不同 preg，WAW/WAR 自动消失（Tomasulo 算法的现代物理寄存器实现，
H&P 第 3 章；对比 R10000/Alpha 21264 的统一物理寄存器堆方案）。

当前有效配置有完整的整数 RAT 和浮点 RAT。源码还保留向量 VRT 接口，但 VRT 输出与
恢复通路包含 dummy 常量，不能把它列为与整数、浮点同等可用的第三套重命名系统。

| 物理寄存器堆 | 规模 | 备注 |
|--------------|------|------|
| 整数 PRF | **96 个物理编号**（preg0 固定，preg1~95 有状态） | 任一时刻需保留 32 个已退休架构映射，其余项才可能供推测写目的分配 |
| 浮点 PRF | **64 项** | 当前标量 F/D 重命名和执行有效 |
| 向量重命名 | 接口与模板保留，当前 VRT/向量 PST 为 dummy 边界 | 不能按 64 项完整向量物理寄存器系统解释 |
| ereg | 扩展状态寄存器 | vsetvl 配置组重命名 |

> **如何理解 96 项整数 preg？** 物理寄存器不是永久切成“32 个架构项 + 64 个重命名项”，
> 而是在 PST 状态机中动态经历 DEALLOC、WF_ALLOC、ALLOC、RETIRE、RELEASE 等状态。任一时刻每个架构
> 寄存器必须有一个已退休映射，其余可释放项形成推测分配余量；实际可容纳多少写目的指令
> 还受旧映射释放、ROB 折叠、发射队列和其他资源限制。

### 5.2 IS 发射：7 个发射队列 + 唤醒-选择

C910 用**分布式发射队列**（distributed reservation stations，对比 Intel P6 的集中式
RS），每类功能单元一个队列，深度 **8~12 项**（访存/分支队列更深，见下表）：

| 队列 | 深度 | 服务单元 | 目标管线 |
|------|------|----------|----------|
| AIQ0 | **8** | ALU0 + DIV + SPECIAL | pipe0 |
| AIQ1 | **8** | ALU1 + MULT | pipe1 |
| BIQ | **12** | BJU 分支 | pipe2 |
| LSIQ | **12** | Load / Store 地址 | pipe3 / pipe4 |
| SDIQ | **12** | Store 数据 | pipe5 |
| VIQ0 | **8** | VFPU0 | pipe6 |
| VIQ1 | **8** | VFPU1 | pipe7 |

> 队列深度并非一刀切：**BIQ/LSIQ/SDIQ 为 12 项、AIQ0/AIQ1/VIQ0/VIQ1 为 8 项**
> （总容量 8+8+12+12+12+8+8 = **68 项**）。这些深度是当前 RTL 的结构事实；
> BIQ/LSIQ/SDIQ 为什么取 12、AIQ/VIQ 为什么取 8 属于设计权衡，可能同时受动态占比、
> 等待时间、端口复杂度和面积影响，不能只凭容量反推出单一设计动机。

**唤醒-选择（wakeup-select）**是乱序调度的心脏：每个队列项里的"依赖项"（`dep_*` 模块）
追踪一个源操作数等待的 preg 号；执行单元产生结果时**广播 preg 号**，所有依赖项比对，
命中则把该源标记就绪（唤醒）；一项所有源就绪即成发射候选，仲裁逻辑按年龄选一条发射
（select）。C910 把唤醒逻辑下沉成可复用的 `dep_reg_entry` 原子模块，是漂亮的模块化。

**分布式 vs 集中式队列的权衡**：分布式队列每个小、端口少、时序好（适合高频），代价是
负载不均（某队列满了即使别处空也得停）。集中式利用率高但端口多、难收时序。C910 选
分布式是高频导向的选择。

> 细节见 [IDU 文档索引](./idu/README.md)（00 至 22 共 23 篇）。

---

## 6. 执行单元 IU / VFPU

### 6.1 8 条执行管线分工

```
pipe0  ALU0 + DIV + SPECIAL   整数运算 / 除法 / CSR / vsetvl   ┐
pipe1  ALU1 + MULT            整数运算 / 乘法·乘累加           ├ IU 整数单元
pipe2  BJU                    分支跳转 / 误预测判定            ┘
pipe3  LSU Load               load 地址生成 + 数据返回         ┐
pipe4  LSU Store-addr         store 地址生成                  ├ LSU 访存单元
pipe5  LSU Store-data         store 数据                      ┘
pipe6  VFPU0                  标量浮点；独享除法/开方           ┐
pipe7  VFPU1                  标量浮点；无除法/开方             ┘ 当前有效 VFPU
```

VFPU 模块名和接口保留了产品向量实现的结构痕迹，但当前顶层 `misa.V=0`，向量
重命名与物理寄存器通路也没有闭环。因此这张当前 RTL 执行图把 pipe6/7 标为标量
浮点管线；产品 Vector 0.7.1 组织在 §6.6 单独说明。

### 6.2 IU 的关键设计

**两条对称 ALU + 长延迟单元搭车**：
- pipe0/pipe1 的 ALU 数据通路由同一 `ct_iu_alu` 模块各例化一次，形成常用整数操作
  双发射的结构条件；实际每拍能否双发仍取决于 AIQ0/1 分流、依赖就绪、端口冲突和
  两条管线各自可接受的操作集合。
- **长延迟单元挂在 ALU 管线上搭便车**：DIV 挂 pipe0、MULT 挂 pipe1，共享发射端口和写回
  端口省面积。代价是 div/mult 发射那拍对应 ALU 不能再发 ALU 指令（由 AIQ 仲裁保证）。

**前递（forward）与写回（writeback）分离**——乱序核实现"1 拍 ALU 延迟"的标准做法：
- ALU 在 **EX1** 就把结果**前递**回前递网络，让 back-to-back 依赖指令下一拍即可发射；
- **EX2** 才**写回** PRF 并通知 RTU 完成。
- 但前递总线是全核时序最紧的网络，所以只有"短指令"（add/逻辑/移位）走 EX1 前递；
  "长指令"（max/min/addsl，走独立选择树到达晚）不进前递、消费者等 EX2 写回（2 拍）。

**乘法器（MULT，4 拍）**：65×65 统一有符号乘法器处理四种符号组合（窄一位换统一）。
**EX3 发 preg 预告、EX4 出数据**——发射队列提前一拍唤醒消费者，数据恰好对齐。乘累加
（MLA）有私有前递链，背靠背 MLA 2 拍一条。

**除法器（DIV，变延迟）**：SRT 基-16 主迭代每轮产生 4 个商位，实际延迟还受除零、
溢出、缓存命中、操作数对齐和余数提前为零影响。2 项结果缓存同时保存同一组输入的商
和余数；`a/b; a%b` 在操作数、符号模式和字宽均相同、表项未被替换且缓存未关闭时，
第二条有很高的命中机会，但不是无条件“必命中”。

**分支单元（BJU）—— "早重定向 + 晚清理"**（全核最重要的时序机制之一）：

```
EX1 判断误预测（方向错 = 实际 ^ 预测；jalr 目标错 = PCFIFO预存值 ≠ src0）
EX2 同时发三枪：
  ① iu_ifu_chgflw   → IFU 立即从正确目标取指（前端抢跑，抢回延迟）
  ② iu_yy_xx_cancel → 杀死 IU 各级错误路径指令
  ③ mispred_stall   → 新路径指令最远只到 ID 级等待
…后端继续跑老指令（错误路径被 cancel 压住）…
分支退休 → RTU flush → rtu_yy_xx_flush（各模块按本地规则恢复/清理）→ 放行新指令
```

为什么还要等退休边界做全局恢复？BJU 的 cancel 能尽早抑制年轻错误路径，但重命名映射、
发射队列、LSU 推测项和已提交 store 各有不同的清除/保留规则，必须由 RTU 在精确边界
统一发出恢复阶段信号。**前端早重定向缩短取指等待，后端按序恢复保证精确状态**。

**PCFIFO（32 项）** 是这套机制的数据载体，也是一处精彩的面积优化：ROB 为省面积不存
完整 PC，控制流预测现场、跳转目标以及链接/AUIPC 类内部操作所需 PC 由 PCFIFO 承接。
IFU 创建项，pipe0/BJU 按 PID 随机读取并由 BJU 回写真实结果，RTU 再按序弹出以重建
退休 PC。带链接的跳转可能占两个 PCFIFO 项，不能把“一条控制流指令”等同于“一项”。

### 6.3 VFPU 详细架构（pipe6 / pipe7）

当前可达的标量浮点执行由 VFALU、VFMAU 和 VFDSU 三类路径组成。pipe6 与 pipe7
都接有加减/比较/转换及乘加流水，但只有 pipe6 接入 VFDSU，所以两条管线从资源
集合开始就不完全对称。源码中仍可见向量宽度、half-lane 和向量操作命名；这些保留
接口必须和 `misa.V=0`、VRT/PST dummy 及实际操作数连线一起解释，不能据此把当前
配置写成双发射向量核。

| 执行路径 | 可到达管线 | 标量操作 | 管线组织 | 延迟口径 |
|----------|------------|----------|----------|----------|
| **VFALU** | pipe6、pipe7 | fadd/fsub、fmin/fmax/fcmp/fclass、浮点转换 | EX1→EX3 | 短路径约 3 拍，仍需按 valid/accept 边界测量 |
| **VFMAU** | pipe6、pipe7 | fmul、融合乘加族 | EX1→EX5 | 短路径约 5 拍 |
| **VFDSU** | 仅 pipe6 | fdiv、fsqrt | SRT 迭代 + 返回流水 | 数据和特殊值相关变延迟 |
| 保留向量/half-lane 接口 | 模板信号可见 | 当前不能作为 RVV 指令路径使用 | 随各子路径保留 | 不形成当前可发布的向量延迟 |

**vfalu 三类算子分工**：`dp_vfalu_ex1_pipex_sel[2:0]` 的 bit0/bit1/bit2 分别选中
FSPU（比较类）/ FADD（加减类）/ FCNVT（转换类），三者共用同一条 EX1→EX3 管线，
ex3_pipedown 时写回 PRF（RTL：`ct_fadd_ctrl.v`、`ct_fspu_ctrl.v`）。

**vfmau 五级 FMA 流水**：控制寄存器 `ctrl_ex5_fma_wb_vld` 在 EX5 边界触发
写回。`dp_xx_ex*_half` 与 `mult*_half_pipedown` 等信号说明模板保留了 half-lane
控制，但在当前向量关闭配置中，它们不能单独证明可执行 SIMD F16；实际可达精度必须
从译码 valid、操作数选择和 `misa` 配置端到端确认。

**VFDSU 是变延迟单元。** `ct_vfdsu_ctrl.v` 根据 double/single/half 类别把
`srt_cnt_ini` 装为 13、6、3，表达不同精度的主迭代预算；但计数初值、SRT 状态驻留拍数
和 RF 接受到结果被消费者使用的端到端延迟不是同一个量。特殊值旁路、余数提前结束、
平方根第二轮控制、返回流水和共享写回时隙都会改变周期数。当前配置还必须先确认相应
精度指令能由译码到达 VFDSU，特别不能把源码中的 half 路径直接当成已开放的 SIMD F16。

**pipe6 独享 vfdsu**，pipe7 没有除法器，因此 fdiv/fsqrt 只能经
VIQ0→pipe6。把单个迭代单元挂在一条管线上减少了重复硬件，但也让 pipe6 的发射
资源同时承载普通浮点和除法/开方入口；是否形成吞吐冲突要看 VIQ0 仲裁、VFDSU
busy 和具体指令混合，不能预先断言“共享不影响吞吐”。

**MTVR 控制入口**：`iu_vfpu_ex1_pipe0_mtvr_vld/inst` 等接口允许 IU 搬运类操作
直接影响 VFPU EX1 控制选择。模块名沿用 move-to-vector-register，但当前有效语义
必须按标量寄存器输入和译码配置解释；它绕过正常 VIQ 发射入口也不等于绕过了所有
有效、冲突和写回协议。

### 6.4 完成 / 写回双轨制：CBUS 与 RBUS

IU 用两条独立总线汇报结果，这是 Smith&Sohi "结果总线 + 完成报告分离"的工业实现：

| | **RBUS（结果总线）** | **CBUS（完成总线）** |
|---|---|---|
| 传什么 | **数据**：64 位结果 + preg 号 | **状态**：cmplt + iid + 异常 |
| 去哪 | IDU 前递网络/PRF 写口、RTU 的 PST | RTU 的 ROB |
| 按什么组织 | 按 **preg**（谁要数据） | 按 **iid**（哪条按序完成） |

**推定完成（presumed complete）**：ALU/MULT/DIV 的选择在 RF 被接受后进入 CBUS
寄存路径，下一拍向 ROB 报告完成；数据真正到达 RBUS/PRF 的时刻由各执行单元和 PST
写回状态另行跟踪。它不是“在 IS 发射拍立即退休”，而是利用这些操作的完成报告与数据
写回可以分离这一协议，允许 ROB 和物理寄存器状态分别推进。

> 细节见 [IU 文档索引](./iu/README.md)（00 至 08 共 9 篇）。

---

## 6.5 指令执行延迟速查表

这是体系结构分析和代码优化最常用的参考。C910 的延迟定义：
**"发射拍到最早能被后继指令消费的拍数"**（含 forwarding 的有效延迟）。

### 整数执行延迟

| 操作 | 管线 | 延迟（拍） | 前递路径 | 备注 |
|------|------|------------|---------|------|
| add/sub/and/or/xor/sll/srl/sra 等 | pipe0/1 ALU | **1** | EX1 前递 | 短指令走 EX1 forward 网络 |
| max/min/addsl/ext/extu/ff1/rev 等 | pipe0/1 ALU | **2** | EX2 写回 | 长选择树晚一拍，无 EX1 forward |
| mul/mulh 系列 | pipe1 MULT | **4** | EX3 广播 preg（EX4 出数据） | EX3 提前一拍唤醒消费者 |
| mulh+mul（MLA 背靠背） | pipe1 MULT | **2**（成对） | EX3 私有前递 | MLA 专属前递链 |
| div（64÷64，典型） | pipe0 DIV | **~20** | — | SRT-16，变延迟；有 2 项结果缓存 |
| rem | pipe0 DIV | **~20** | — | 与 div 同时完成，配对免费 |
| 分支（BEQ/BNE/JAL/JALR 等） | pipe2 BJU | EX1 执行检查、EX2 纠错重定向 | — | 预测发生在 IFU；误预测损失还受发现位置、stall、目标路径和恢复等待影响 |
| CSR read/write | pipe0 SPECIAL | 见注 | — | CSR 读写可能需要 flush 后端 |
| vsetvl | pipe0 SPECIAL | flush | — | 改向量配置需 flush + 重命名 ereg |

### 访存延迟

| 操作 | 延迟（拍） | 条件 |
|------|------------|------|
| load L1 命中 | **4**（AG→DC→DA→WB） | 正常 cache 命中，完整 4 级流水 |
| load L1 命中 + store 前递 | **4** | SQ 前递与 cache 命中同延迟 |
| load store 重发（discard） | **4 × 2 ≈ 8**+ | 部分覆盖/数据未就绪时重发 |
| load L1 miss，关键字优先 | **L1 miss + L2 hit ≈ 12~20** | 关键字先交付，load 不等整行 |
| store（退休后落 cache） | 退休后异步 | store 不在管线中写 cache，由 WMB 写 |

### 浮点/向量延迟

| 操作 | 延迟（拍） | 管线 | 备注 |
|------|------------|------|------|
| fadd/fsub | **3** | pipe6 或 7 vfalu | EX1→EX2→EX3 |
| fmul | **5** | pipe6 或 7 vfmau | EX1→EX5 |
| fmadd/fmsub/fnmadd/fnmsub | **5** | pipe6 或 7 vfmau | 单操作完成融合乘加 |
| fmin/fmax/fcmp/fclass | **3** | pipe6 或 7 fspu | 与 fadd 同流水 |
| float 类型转换（fcvt 系列） | **3** | pipe6 或 7 fcnvt | 同流水 |
| fdiv（f64 双精度） | 常见约 **18** | pipe6 vfdsu | 计数初值、特殊值和余数提前结束会改变端到端周期 |
| fdiv（f32 单精度） | 常见约 **11** | pipe6 vfdsu | `13/6/3` 是计数器装载值，不能直接写成固定迭代轮数 |
| fdiv（f16 半精度） | 常见约 **8** | pipe6 vfdsu | 当前是否开放 f16 指令还要结合译码配置 |
| fsqrt | 数据相关变延迟 | pipe6 vfdsu | `srt_secd_round` 不等于所有输入都固定执行两遍完整迭代 |

表中短流水数字描述执行级或最早旁路边界，不是所有指令从 IS 发射到消费者的固定总延迟。
VFDSU 还受特殊值、提前结束、起止拍定义和共享返回时隙影响，准确延迟应直接在波形中以
输入接受、SRT 状态、结果 valid 和消费者接受四个时刻测量。完整向量 F16 SIMD 在当前
开放配置不可作为有效性能路径引用。

---

## 6.6 产品向量架构与当前开放 RTL 的边界

官方 C910 产品论文描述的是 RISC-V Vector 0.7.1 时代的 128-bit 向量实现，不兼容最终
RVV 1.0。当前仓库开放 RTL 则采用标量 F/D 配置：`misa.V=0`，上游向量总判定关闭，
向量 VRT 和向量 PST 含 dummy 常量路径，VFPU 顶层也没有获得完整 VR0/VR1 操作数。
因此下面的 VLEN、LMUL、VMB 和双向量管线内容用于解释产品材料和源码保留接口的设计
意图；只有标量 F/D 执行、部分 vsetvl 配置计算和明确接通的控制路径可以在当前顶层
波形中按有效功能观察。

### 6.6.1 向量寄存器与数据宽度

| 项目 | 产品/模板所表达的组织 | 当前开放 RTL 边界 |
|------|----------------------|-------------------|
| **VLEN** | `ct_vfpu_dp` 中保留 128-bit 参数与 16-byte `vlenb` 相关接口 | `misa.V=0`，不能据此宣称完整向量 ISA 可执行 |
| 架构向量寄存器 | 模板按 v0~v31、SEW 8/16/32/64、LMUL 分组组织 | VRT 恢复和映射输出没有完整有效状态 |
| 向量物理数据 | `vregfile_fr/vr0/vr1` 名称和端口表达标量低部与向量片段 | 当前 VFPU 有效输入使用标量 FREG，VR0/VR1 未形成完整端到端通路 |
| vstart/掩码/VMB | 控制字段和模块接口仍可见 | 必须逐条检查上游 valid、实例和非 dummy 返回，不能仅按端口名认定可用 |

从产品微结构角度看，共享浮点低部和向量片段可以复用标量/向量数据通路；从当前
开源实现角度看，`vregfile_fr` 的标量 F/D 路径有效，而 `vr0/vr1` 接口存在不等于完整
128-bit 向量寄存器系统已经接通。面积节省也不能由模块命名直接量化。

### 6.6.2 配置重命名：vl / vtype 也要进重命名表

产品向量机制中，向量指令的行为取决于当前 `vl`（向量长度）和 `vtype`
（SEW/LMUL）。乱序执行时，一条向量指令必须关联它所依赖的配置版本，而不能简单读取
“当前最新值”。C910 模板把 vl/vtype 当作一种可重命名状态，使用 32 个物理 ereg；
vsetvl 分配新版本，后续指令引用对应 ereg，从而把隐式配置先后关系转化为显式依赖。
当前开放 RTL 的 SPECIAL 单元确有 vsetvl/vsetvli 计算与预测校验逻辑，EREG 也有状态
路径，但完整向量消费者和 VRT 未接通，所以这套方法可以作为机制学习对象，不能当作
当前 RVV 程序已可执行的证明。

这种做法的通用意义是把隐式全局状态显式化为可重命名版本，使调度器能够像处理普通
数据依赖一样处理配置依赖。它适用于 vl/vtype 一类会影响大量后续指令的状态，但是否也
适用于 `frm` 等其他 CSR，必须查看具体实现是否真的分配版本，不能仅凭方法类比。32 个
ereg 的存储、端口和性能收益仍需以完整产品实现或恢复相应数据通路后的结果量化。

### 6.6.3 向量执行与访存

- 产品/模板组织以 VIQ0→pipe6、VIQ1→pipe7 表达双管线向量调度，并保留 vmla
  跨管线前递接口；当前有效执行是两条标量浮点管线，不能把接口宽度直接换算成两条
  128-bit 向量指令同拍执行。
- LSU 中保留 VMB 与向量访存相关接口，用于表达按 vl/vsew/stride 拆分访问的思路；
  当前文档没有证明完整 VMB 端到端路径在本配置可达，研究时应从译码 valid 和实例连线核实。
- mtvr 控制接口可旁路 VIQ 注入 VFPU EX1，但必须结合当前标量数据选择判断实际有效操作。

### 6.6.4 与 RVV 1.0 的关系

产品材料中的早期向量扩展包含 LMUL、掩码、vstart、widening/narrowing 和规约等概念，
但编码与 RVV 1.0 不兼容。其教学价值在于说明浮点数据通路复用、配置状态重命名和向量
访存拆分如何组合；当前仓库的结论则应限定为“保留了这些结构痕迹，标量 F/D 路径有效，
完整向量端到端路径未开放”。

---

## 7. 访存子系统 LSU

访存是高性能核最难的部分——它要同时做到：乱序执行 load/store、保证内存顺序语义、隐藏
cache miss 延迟。C910 LSU 用一组队列家族解决。

### 7.1 三条访存管线，4 级流水

| 级 | load (pipe3) | store-addr (pipe4) | store-data (pipe5) |
|----|--------------|--------------------|--------------------|
| AG | 算虚地址 VA + MMU 翻译得 PA + 生成 `bytes_vld` | 同左 | 接收 store 数据 |
| DC | 读 tag、查 SQ/WMB 依赖、创建 LQ 项 | 读 tag、创建 SQ 项、反查 LQ | 按 sdid 写进 SQ |
| DA | tag 比较定命中、数据多路选择、符号扩展、miss 申请 RB | 命中判定、更新 dirty | — |
| WB | 数据写回 PRF + 报 RTU 完成 | 仅报完成（store 不在此写存储器） | — |

**为什么 store 拆地址(pipe4)/数据(pipe5)两条管线？** 地址早算让后续 load 尽早做依赖
判断；地址或顺序条件未满足时，LSIQ 对相关 load 设置等待/重发原因，而不是把所有情形
都概括成“未知 store 无条件挡住全部年轻 load”。store 数据可以晚于地址到达，因为
未提交 store 先保存在 SQ，退休后才进入必须完成的提交域。地址/数据解耦使依赖判断
和数据准备可以分别推进。

### 7.2 队列家族（每个数字都有讲究）

| 队列 | 项数 | 域 | 职责 |
|------|------|-----|------|
| **LQ** Load 队列 | 16 | 推测 | 记录在途 load，检测 RAW/RAR 顺序违例 |
| **SQ** Store 队列 | 12 | **推测域** | 已执行未提交的 store，向 load 前递数据 |
| **WMB** 写合并缓冲 | 8 | **已提交域** | 退休后的 store，合并相邻写后落 cache/总线 |
| **RB** miss 请求缓冲 | 8 | — | 登记 cache miss 并发 BIU 请求；它是 MLP 上限之一，不是唯一决定因素 |
| **LFB** 行填充缓冲 | addr 8 / data 2 | — | 接收 refill 数据写回 cache |
| **VB** 逐出缓冲 | 2 | — | 暂存被替换的脏行 |
| **PFU** 硬件预取 | PMB/SDB/PFB(8 流) | — | 检测访存流，提前取数 |
| **SNQ** 侦听队列 | 6 | — | 处理外核一致性侦听 |

### 7.3 三大正确性机制

**① store→load 前递（forwarding）**——三步协议：
```
ld_dc 广播 PA+bytes_vld+iid → SQ 12 项并行比：比我老？同 16B？字节相交？数据就绪？
三种结论：
  可前递（唯一最年轻的重叠老 store）→ ld_da 拍独热选数，与 cache 命中同延迟
  重叠但数据没到 / 部分覆盖    → discard：load 回 LSIQ 重发
  无关                          → 走 cache
```
其中 `bytes_vld[15:0]`（16 字节窗口字节位图）是 LSU 内一切依赖比较的"通用语言"——比
地址区间比较更直接地表达逐字节覆盖。部分覆盖或数据未就绪时采用 discard/replay，
避免在当前关键路径上构造任意多 store 与 cache 数据的复杂拼接。它出现得是否少、
代价是否可接受必须由具体 workload 的计数器和波形量化，不能由 RTL 结构直接断言。

**② 顺序违例检测**——load 乱序执行越过了更老的 store，读到旧值（RAW）。store 到达时
反查 LQ，发现“更年轻 + 同 16B + 字节相交 + 已取得数据”的 load 后，上报 spec-fail
恢复信息并在精确边界重执行。这里有两套不能混为一谈的状态：IFU 的 `ct_ifu_sfp`
按取指 PC 保存 Store-Fetch Prediction 信息，用于影响后续 load 的 no-spec 属性；
LSU 的 `ct_lsu_spec_fail_predict` 本身没有 PC 输入，而是围绕一次冲突保存 store
地址块、字节掩码和 IID，在恢复窗口内产生 `sf_spec_mark/hit`。两者共同服务内存依赖
推测，但不是一张按 load PC 长期训练的 LSU store-set 表。

**③ 推测域 / 提交域分离**——SQ 是推测域（flush 可清），WMB 是已提交域（flush 不可清，
体系结构已承诺必须写完）。这正是 RTU flush 状态机等待 `lsu_all_commit_data_vld` 的对象。
**这是精确异常在访存侧的体现：已提交的写不能回滚，未提交的写必须能撤销**。

### 7.4 非阻塞 cache 与延迟隐藏

```
load miss → RB(8项)登记 → BIU AXI 读：
  ├─ 关键字优先(critical word first)：首拍数据直接交付 ld_wb（load 延迟只到首字！）
  └─ 整行 → LFB → refill 写 cache → 被替换脏行 → VB → AXI 写回
```

- **并行 miss 跟踪上限之一是 8 项 RB/LFB 地址项**：该容量允许多个未完成 line
  请求共存，但实际 MLP 还受 LQ、依赖链、TLB、LFB data entry、总线 ID/信用和下层带宽限制，
  不能直接写成恒定 `MLP=8`。
- **关键字优先**：不等整行 64B 到齐，先把 load 要的那个字交付，大幅缩短 miss 延迟。
- **LFB addr 多(8)/data 少(2)**："账多本、钱少柜"——同时跟踪的 miss 多（便宜），同时
  正在收 64B 数据的少（贵的缓冲省着用）。
- **硬件预取 PFU**：PMB（按 PC 配对算 stride）→ SDB（确认流）→ PFB（8 条活跃流），
  L1/L2 双状态机分别领先 demand 近/远距离。机会主义低优先级，资源紧张直接丢。

> 细节见 [LSU 文档索引](./lsu/README.md)（00 至 10 共 11 篇）。

---

## 7.5 内存顺序模型（RVWMO）与 fence / AMO 的微架构实现

### 先理解"内存一致性模型"在说什么

这是体系结构最烧脑、也最容易被略过的概念，先把它讲透。单核看自己的内存访问，总是符合
程序序（有数据依赖就等）。**但多核下，一个核的 store，别的核"什么时候、以什么顺序"看到，
就成了大问题**。"内存一致性模型"就是**硬件给软件的一份契约：在没有显式同步时，跨核访存
最多能被重排到什么程度**。两条路线：
- **强一致（如 x86 TSO）**：对外几乎按程序序可见，软件省心，但硬件要时刻压制重排、损失性能；
- **弱一致（RVWMO / ARM）**：默认放开重排（store 可晚落、load 可早跑），**只在你用 fence
  或原子指令显式要求时才保证顺序**——把"要不要顺序"的决定权交给软件。

乱序 load/store、store buffer 和非阻塞 miss 都会使微架构执行顺序不同于程序序。
内存模型规定哪些差异允许被其他 hart 观察到，硬件还必须在允许范围内维持同址顺序、
数据/地址/控制依赖、fence 与 acquire/release 等 preserved program order。较宽松的模型
给乱序引擎更多隐藏延迟的空间，但把正确同步的责任交给 ISA、编译器和并发库共同承担。
LSU 的顺序违例检测解决本 hart 推测执行读错值后的恢复，fence/aq/rl 则约束体系结构
可见顺序；二者相关，但不能简单概括成“单核顺序”和“跨核同步”的二分。

### RVWMO 基本约束

RISC-V 基础内存模型是 **RVWMO（RISC-V Weak Memory Ordering）**。它不是“只有显式
fence 才有序”：规范还定义同址访问、数据依赖、地址依赖、部分控制依赖、成功的
LR/SC、AMO 的 `aq/rl` 标记以及 fence 所建立的 preserved program order。没有被这些
规则约束的访问才可能以不同于程序序的顺序对其他 hart 可见。

C910 对 RVWMO 的实现要点：

| 场景 | C910 保证 | 机制 |
|------|-----------|------|
| load 越过地址尚未确定的更老 store 后发生同址冲突 | 必须恢复到符合 RVWMO 的结果 | SQ/LQ 字节重叠检查、spec-fail 与 replay |
| 已提交 store 的完成 | flush 后仍必须继续 | SQ 推测域与 WMB 提交域分离 |
| 未被依赖、同址、fence 或 aq/rl 约束的访问 | 可被其他 hart 观察为不同顺序 | 乱序执行与缓冲；软件按同步协议加约束 |
| `fence rw,rw` | 前驱集合中的读写先于后继集合中的读写被观察 | fence 类型相关的 LSU 等待/完成协议 |
| fence.i（指令 cache 一致性） | 屏障后能看到屏障前所有 store 的最新指令 | 排空 + I-Cache 全无效 |
| sfence.vma | 按 rs1/rs2 语义使相应地址翻译失效，并与页表修改排序 | MMU tlboper 扫描 JTLB、清 uTLB、协调在途 PTW |

### fence 指令的执行流程

fence 会限制访存调度并等待规定范围内的更老事务达到所需可见点，因此在写缓冲或
miss 较多时可能形成显著串行化。当前 RTL 的完成条件分散在 RB、WMB、LSIQ/SDIQ、
页面属性和具体 fence/sync 类型中，不能机械写成“所有 fence 都等五个队列全空”。

```
fence/sync 微操作进入 LSU
  -> RB/WMB 等结构记录相应排序状态
  -> `lsu_has_fence` 表示存在未完成 fence 事务
  -> `lsu_idu_wait_fence[11:0]` 对具体 LSIQ 项设置等待原因
  -> 各来源按 fence 类型、页面属性和旧事务状态推进
  -> 规定的完成条件满足后，清除等待并允许后续访存继续
```

**fence.i 额外步骤**：还要通过 IFU 接口发送 I-Cache 全无效（flush）信号。
完成后，IFU 从正确 PC 重取指，保证取到 dcache 已提交的新指令内容。

**sfence.vma 额外步骤**：MMU `tlboper` 根据 rs1/rs2 组合执行全清、按 ASID、
按 VA 或按 VA+ASID 失效。JTLB 维护可能逐组扫描和比较，uTLB 因不保存 ASID 而
在多数地址空间维护中整清；在途 PTW 还要被协调或重启。`ct_lsu_mcic` 只是把 PTW
的 PTE 读取接入 D-cache/RB/BIU，不是 cache invalidate 控制器，也不存在
“sfence 为 D-cache VIPT 别名做清理”的当前实现，因为本核 D-cache 是 PIPT。

**性能观测**：PMU event41 对“fence stall 或 fence/sync valid”先 OR 后按周期计数，
event40 则是 IDU 的 backend-stall 事件。两者口径不同，不能互相替代；评估 fence
代价应同时看 event41、WMB/RB 等状态和退休空洞。

### AMO 与 LR/SC 的本地监视器（Local Monitor）

C910 的 `ct_lsu_lm.v` 维护 LR/SC 的保留状态和 AMO 期间的局部 cache-index 互锁，
但完整原子事务还跨 load/store、SQ/WMB、cache ownership 与总线响应。状态机可按
下列实现阶段理解：

```
IDLE
  -> LR/AMO 发射，记录地址、大小和页面属性
WAIT_REQ
  -> 判定本地完成，或等待 RB 为总线读分配事务 ID
WAIT_RESP
  -> 等待与记录 ID 匹配的返回
EX_WAIT_LOCK
  -> LR 保留有效，等待 SC 或使保留失效的事件
AMO_LOCK
  -> AMO 对相关 cache index 的局部互锁期，WMB 完成原子写后解锁
```

`lm_sq_sc_fail` 的直接成功前提包括处于 `EX_WAIT_LOCK`，且 SC 的完整物理地址和
访问大小与 LR 记录相同。相同行 snoop invalidate、本核 victim evict、flush、异常和
相关控制事件都可能使保留失效。AMO_LOCK 还会按 cache index 阻断 load DA、store DA、
SNQ 与 PFU 的冲突，说明这里同时管理“体系结构保留地址”和“实现层阵列互锁”。

RISC-V 只对满足规范约束的 LR/SC constrained loop 提供最终进展保证，并不保证任意
LR/SC 序列在固定步数内成功。软件必须把 SC 失败作为正常结果重试；是否存在饥饿还取决于
程序是否满足约束、其他 hart 的竞争和平台实现。

---

## 8. 存储层次：Cache 与 MMU

存储层次的全部哲学是一句话：**用小而快的层吸收绝大多数访问，用大而慢的层兜底**
（H&P 第 2 章「存储层次」）。C910 是 L1（I/D 分离）→ L2（统一共享）→ 主存三级，
地址翻译则是 uTLB → JTLB → 硬件 PTW 三级——**TLB 体系本身就是"cache 思想在地址
翻译上的复刻"**。下面逐层把每个阵列的几何、替换、保护、以及"为什么这么取舍"讲透。

### 8.1 三级 Cache 总览

| 层级 | 容量 | 组织 | 行 | 索引 | 替换 | 保护 | 备注 |
|------|------|------|-----|------|------|------|------|
| L1 I-Cache | 64KB | 2 路 / 512 组 | 64B | **VIPT** | 两路轮转 | 当前 ECC 路径未完整启用 | Predecode 阵列、way-predict、per-bank 门控 |
| L1 D-Cache | 64KB | 2 路 / 512 组 | 64B | **PIPT** | 状态阵列 bit6 控制候选路 | 当前 ECC decoder 注释并绑 0 | 双 tag 阵列、全 cache 共 8 个 32-bit data bank、cache_buffer |
| L2 Cache | 1MB | 16 路 / 1024 组 | 64B | PIPT | 每组 one-hot 轮转并旁路 pending 路 | ECC 框架保留，开源纠错核未落地 | 2 sub-bank、可编程延迟、I/TLB next-line 预取 |

**三级 cache 都没有实现命中更新的真 LRU，但具体状态编码并不相同。** I-Cache
使用两路轮转状态，D-Cache 由 dirty/status 阵列 bit6 参与替换选择，L2 则保存每组
16 位 one-hot 轮转字段，并在当前候选 pending 时改选最低编号非 pending 路。它们共同
特点是普通命中不维护完整最近使用次序，但不能统一概括成“每组只需
`ceil(log2(way))` 位 FIFO”。替换状态的面积、时序和命中率影响需要综合与 workload
测量，RTL 只能确定更新规则。

**I-Cache 用 VIPT、D-Cache 用 PIPT——两类访问不同权衡的经典对照**：
- 取指对延迟敏感，VIPT 允许用页内虚地址位发起阵列访问，并与地址翻译重叠，避免把
  完整 TLB 翻译和 SRAM 访问强制串行；实际节省多少周期仍取决于流水边界。
  VIPT 的同义地址约束仍要由 cache 维护和系统软件正确处理，不能因为 I-Cache 只读
  就认为别名天然无害。
- 访存有 store、会写脏，PIPT 用物理地址做 index+tag，**根除别名**（同一物理地址经不同
  虚地址映射也落同一行），正确性优先。

### 8.2 L1 I-Cache 内部（RTL: `ifu/rtl/ct_ifu_icache_*`）

**Tag 阵列（59 bit / 组，开 ECC 时 61 bit）**：一个表项同时存两路 + 替换位：
```
tag_entry[58:0] = { fifo(1) , way1: valid(1)+ptag(28) , way0: valid(1)+ptag(28) }
                    ▲替换指针   ▲————29 bit————▲       ▲————29 bit————▲
```
- **物理 tag 28 bit**（PA[38:11]）——VIPT 故 tag 用物理位。
- **fifo 1 bit/组**就是替换指针：refill 后翻转，指向下次要替换的路。
- 开 `L1_CACHE_ECC` 时每行加 2 bit ECC（SRAM 从 59→61 bit）。

**Predecode 阵列——为 RVC 变长边界预付出的代价**（详见 §4.3）：refill 时对每个
16-bit 槽预解码出 **4 bit**：`{绝对跳转(JAL/C.J)、是否分支、指令边界 bry1、bry0}`，
存进 cache。因为 RVC 让指令边界是动态的（16/32 位混排），不预解码就得在每个 IP 级
重算边界、还要跨拍。预解码把这件事一次性在 refill 时做完（组合逻辑现算现存），
**用 refill 时的一点功耗换取每拍取指的关键路径**（Shen & Lipasti「预解码」思想）。

**数据阵列 + per-bank 门控**：每路数据 = 4 个 32-bit bank，一路读出恰好 128 bit
（= 一个取指组）。每个 bank 有独立 `cen/clk_en`，配合 way-predict 只唤醒**目标那一路、
目标那几个 bank** 的 SRAM——这就是 §4.3 说的省功耗来源的 RTL 落点。

**way-predict 是 2-bit 即时信号、不是表**：`{way1_有效预测, way0_有效预测}`，
取指那拍现算（来自 L0-BTB/分支现场或行内顺序推断），不存独立 SRAM：
- `2'b10/2'b01`：只读预测命中的那一路 → 省一半 Data 阵列读功耗；
- `2'b00`：没有可用单路预测时，控制逻辑进入 stall/reissue 路径并补读所需数据。
单路预测错误也会触发 reissue。局部控制中可见一拍状态推进，但端到端额外周期还受
当时 IF/IP stall、refill 和更高优先级 PC load 影响，不应固定写成“惩罚仅 1 拍”。

**refill 路径**：BIU 以 128-bit beat 返回可缓存 line 数据，四个 beat 组成一个
64B 行。L1 refill 状态机在首个关键 16B beat 到达时可以向当前取指旁路，但只有
四个 beat 收齐并完成 WFD 发布后，整条 line 才置为 cache 中可复用的有效项。

### 8.3 L1 D-Cache 内部（RTL: `lsu/rtl/ct_lsu_dcache_*`）

**双 tag 阵列——load/store 同拍并行的物理基础**：
| 阵列 | 宽度 | 服务 |
|------|------|------|
| `ld_tag_array` | 54 bit / 组 | load 管线（pipe3）查命中 |
| `st_tag_array` | 52 bit / 组 | store 管线（pipe4）查命中 + 替换 |

两份 tag 保存相同的 26-bit 地址 tag，但 load 副本还带每路 valid，store 侧 valid
来自 dirty/status 阵列。复制读口结构让 load 与 store 可以分别提出 tag 请求，减少
对单一 SRAM 读口的直接争用；实际并行能力还受 data bank、仲裁和流水反压限制。

**全 cache 共 8 个 data bank（每 bank 2048×32 bit）**：bank0~3 形成 low 128-bit
窗口，bank4~7 形成 high 128-bit 窗口。两个 way 和 16B 位置共同编码进 11-bit SRAM
index，不能把组织简化成“每个 way 各有 8 bank”或“仅由 PA[5:3] 直接选择一条 64B
line 的 bank”。不同来源能否同拍访问，必须对每个 bank 分别检查请求、选择、index 和
active-low SRAM 使能。

**dirty 阵列（512×7 bit）独立于 tag**，每组 7 bit 一次给出两路的全部元数据 + 替换位：
```
dirty_entry[6:0] = { way_ptr(1) , way1{dirty,share,valid} , way0{dirty,share,valid} }
```
替换时读 `way_ptr` 定受害路、读对应 `dirty` 定要不要写回。把 dirty/valid/share 单独
成阵列（不挤进 tag）是为了**替换决策一次读全**、且 store 更新脏位不必动 tag SRAM。

**cache_buffer（一个 `PA[39:4] + 128-bit` 近期数据项）**：它服务 `acclr_en` 的跨
16B 边界加速路径。后续子访问的 `addr1` 命中时，DA 可以按 `bytes_vld1` 把 buffer 字节
与当前 cache 数据合并；D-cache 同 index 写、cache disable、ICC/no-op 等条件会使其失效。
它不是通用 victim buffer，也不能概括成命中后必然省去一次 L2 miss。

**D-cache 仲裁按资源分别完成。** load/store tag、dirty 阵列和八个 data bank 的
候选源与选择条件不同，不存在一条覆盖所有资源的统一优先级。LFB、VB、SNQ、ICC、
WMB、MCIC/PTW 和普通 AG 请求可能竞争某些端口，也可能在不冲突资源上并行。判断某源
是否真正访问阵列，必须查看对应资源的 select/grant、最终 SRAM 使能和 index，而不能
只根据源 `req` 或一张总排序表推断。

### 8.4 L2 Cache 内部（RTL: `l2c/rtl/ct_l2c_*`）

**几何：1MB / 16 路 / 64B / 1024 组，物理上拆成 2 个 sub-bank**——每个 sub-bank
有 512 个 local set 和独立的 tag/cmp/data/wb 流水。CIU 用 `PA[6]` 选择 bank，因而
相邻 cache line 可交替进入两侧。双实例提供 bank 级并行的结构条件，但实际吞吐取决于
地址分布、CIU/SNB 仲裁、同 bank 冲突和返回资源，不能写成固定翻倍。容量关系仍为
`16 way × 1024 set × 64B = 1MB`，每个 sub-bank 的 tag index 为 9 位。

**普通请求主路径**：`tag（读 tag/status）→ cmp（命中归约、状态更新、选 victim）→
data（按需读写整行数据）→ wb（组织响应）`。ICC 是每个 sub-bank 的维护/DCA 引擎，
会等待普通流水排空后接管 SRAM 端口，不是所有普通访问都经过的第五流水级。

**可编程访问延迟——一份 RTL 适配不同速度的 SRAM**：tag/data 阵列的访问拍数由 CIU 寄存器
`ciu_l2c_tag_setup + tag_latency[2:0]`、`data_setup + data_latency[2:0]` 配置（tag 最多
~5 拍、data 最多 ~9 拍）。这让同一套 L2 RTL 能配合不同工艺/频率下不同时序的 SRAM 编译宏，
是商用 IP 的典型可配置性。

**ECC 结构是保留框架，不是当前开源版已经工作的 SECDED。** `ct_l2c_tag_ecc.v`、
ICC 的纠错状态、wb 的 ECC rfifo 以及 data ECC 端口都可见，但有效纠错核未开源，
错误输入固定旁路，相关状态不可达。tag/status 或 data 位翻转在架构上确实需要保护，
但当前 RTL 不能据此宣称已完成检测、纠正和不可纠错异常处理。

**轮转替换**：每组保存 16-bit one-hot 候选状态，回填时推进；当前候选 pending 时，
逻辑改选最低编号的非 pending way。它不是命中更新的 LRU，也不是简单 4-bit 二进制指针。

**状态位 status[7:0] = {cp[3:0], valid, shared, dirty, pend}**。`cp[3:0]`
记录一致性参与者的 presence 线索，供 CIU 缩小侦听候选；逐 PIU 的最终目标和协议状态仍由
CIU/SNB 根据事务类型决定。四位宽度表示接口编码空间，当前顶层只实例化 core0/core1，
不能直接解释成四个核心都已接入并验证。

**预取与写回**：L2 自带**指令预取（iprf）+ TLB 预取（tprf）**两类预取（`ct_l2c_prefetch.v`），
与 LSU 的数据预取（§7.4 PFU）分工互补。L2 的 wb（写回/响应）级用一个 **3 项 rfifo**
（每项 14 位 = resp+cp+sid，`ct_l2c_wb.v`）缓存"无数据的纯响应"（写完成/维护完成等），
与带数据的响应在输出端二选一——把"慢数据"和"快响应"解耦。

**共享范围**：cp 位图和 PIU 接口保留四个参与者编码，当前 `openC910.v` 有效私有核心
路径为 core0/core1，其余槽位包含常量或未接入边界。L2 是当前双核一致性路径的共享层，
不能只按位图宽度宣称完整四核集成。

### 8.5 MMU 内部（Sv39，RTL: `mmu/rtl/ct_mmu_*`）

**虚拟内存仅单阶段 Sv39**（39 位虚地址、三级页表、4K/2M/1G 三种页）——无 H 扩展的
两阶段（G-stage）翻译。三级 TLB 把"翻译"也做成存储层次：

| 结构 | 规格 | 替换 | 表项内容 | 备注 |
|------|------|------|----------|------|
| **I-uTLB** | 32 项，全相联 | tree-PLRU | valid+VPN(27)+pgs(3)+PPN(28)+flag(14) | 四个固定 fast slot 驱动快速 PA MUX，slow hit 与轮选 fast slot 交换 |
| **D-uTLB** | 16 普通 + **1 个 1GB 专用项** | tree-PLRU（仅 16 普通） | 普通项保存 4KB 及 2MB 映射的当前 4KB 子页 | 双端口；1GB 项单独比较高位 |
| **JTLB** | **1024 = 256 组 × 4 路** | 回填推进的 one-hot 轮转 | valid+VPN(27)+**ASID(16)**+pgs(3)+global(1) / PPN(28)+flag(14) | SRAM 实现，按 4K→2M→1G 逐尺寸搜索 |
| **PTW** | 单发遍历 | — | — | 3 级 Sv39、~20 态 FSM、走 LSU 访存 |

uTLB 的 tree-PLRU 会在命中时更新路径状态，iuTLB 与 duTLB 分别使用 31 位和 15 位树。
JTLB 每组保存 4 位 one-hot 轮转状态，只在成功回填时选路并推进，普通命中不更新。
`jtlb_xx_fifo[11:0]` 是一次多页尺寸查找的临时汇总，不是 12 项 FIFO。两类策略的
控制差异可以从 RTL 确认，哪一种对特定 workload 的命中率或 PPA 更好仍需测量。

**ASID 16 位只存在 JTLB，uTLB 不存 ASID——一个漂亮的上下文切换优化**：进程切换换
ASID 时，小小的 uTLB 直接整体作废（便宜），而**大的 JTLB 因为每项带 16 位 ASID 标签，
不同进程的翻译可以共存**、切回来无需重填（贵的东西保住）。再加 global 位标记内核公共
映射；JTLB 比较时 global 项旁路 ASID 条件，但仍须满足 valid、VPN 和页大小等命中条件。
这是"ASID 避免每次切换全清 TLB"经典思想（H&P 第 2 章）的分层
分层实现。

duTLB 的独立 huge 项只保存 1GB 翻译。2MB 翻译会结合当前访问地址专门化为对应的
4KB 子页后进入 16 个普通项，因此不能把专用项写成同时保存 2MB/1GB。该组织使普通项
保持统一比较形式，但比较器面积和命中率收益仍需实现与 workload 数据支持。

**PTW 走 LSU、无独立 PTE cache**：uTLB/JTLB 全 miss 时，PTW 状态机（~20 态：
FST→SCD→THD 三级 + PMP 检查 + 大页跨界处理）发起页表读，**请求经 `mmu_lsu_data_req`
走正常 LSU/cache 层次**（故页表本身也享受 D-Cache/L2 缓存），一次只跑一个遍历。
I 侧 / D 侧 / sfence 操作经 `ct_mmu_arb.v` 仲裁共享 JTLB 与 PTW。

**XMAE 内存属性（14-bit flag/项）**：除标准 V/R/W/X/U/A/D 权限位外，C910 在 flag 里
额外携带 **SO（strong-order 强序）、CA（cacheable）、BUF（bufferable）、SEC（安全）、
SH（shareable）** 等属性位——这就是 XMAE（XuanTie Memory Attribute Extension）对
RISC-V PMA 的私有增强：**把"这段内存能不能缓存/能不能合并写/是否强序"直接编进页表项**，
比纯靠物理地址区间判定更灵活。

**sysmap（硬连线 PMA 映射）**：`ct_mmu_sysmap.v` + `sysmap.h` 定义 8 个半开物理
地址区间及其 5-bit 属性。属性来源由 MAEE 显式选择：`maee=1` 使用 PTE XMAE，
`maee=0` 使用 sysmap；它不是在页表字段为空或“未覆盖”时自动 fallback。PTW 还用
sysmap 边界判断未启用 MAEE 时的大页是否跨越不同 PMA 区域。

### 8.6 存储层次设计权衡小结

| 设计点 | C910 选择 | 取舍 / 动机 | 教科书坐标 |
|--------|-----------|-------------|-----------|
| 三级 cache 替换 | 均不是真 LRU；I、D、L2 使用各自轮转/状态规则 | 更新条件和状态位宽不同，收益需实测 | H&P 2 章替换策略 |
| I/D 索引 | VIPT / PIPT 分用 | 取指省周期 / 访存避别名 | H&P 2 章 cache 别名 |
| I-Cache 预解码 | refill 时预解码 4bit/槽 | 解决 RVC 变长边界、卸下取指关键路径 | Shen&Lipasti 预解码 |
| I-Cache way-predict | 2-bit way 使能；00 自停，01/10 单路，11 双路 | 单路可减少翻转，误选经 reissue；功耗收益需实现报告 | H&P way prediction |
| D-Cache tag | 双副本 tag 阵列 | 面积换 load/store 同拍并行 | 端口 vs 副本权衡 |
| L2 结构 | 2 sub-bank × 512 组 | 提供 bank 级并行条件，不保证吞吐固定翻倍 | 多 bank 交织 |
| L2 延迟 | 可编程 tag/data 拍数 | 一份 RTL 适配多种 SRAM | 商用 IP 可配置 |
| L2 保护 | tag/data ECC 框架保留，当前开源纠错路径旁路 | 不能按已实现 SECDED 计入功能或性能 | H&P 可靠性 / SECDED |
| TLB 层次 | uTLB 命中更新 tree-PLRU；JTLB 回填推进 one-hot 轮转 | 小表与 SRAM 后备表使用不同更新规则 | H&P 2 章 TLB |
| ASID | 仅 JTLB 带 16-bit | 切换时清小表、保大表 | H&P ASID |
| 大页 | D-uTLB 专设 huge 项 | 普通项保持简单、大页有位置 | 多页大小 TLB |
| PTW | 走 LSU、无 PTE cache | 页表也享 cache、省一套结构 | 硬件页表遍历 |
| 内存属性 | XMAE 编进页表项 | 比纯物理区间判定灵活 | RISC-V PMA 增强 |

---

## 9. 退休与精确异常 RTU

乱序核把指令打乱执行，但体系结构状态必须表现得像顺序执行——这是 RTU 的使命，也是
精确异常（precise exception）的实现（Smith&Sohi 的经典议题）。

### 9.1 IID：贯穿全核的指令身份证

**7 位 IID = 1 位 wrap + 6 位 ROB 物理索引**。低 6 位选择 64 个 ROB 表项，最高位
区分相邻两圈。年龄比较在一个有效 64 项环形窗口内结合 wrap 与低位完成，使 BJU、LSU
和发射队列无需直接读取 ROB 头尾指针。这里的窗口约束针对 64 个活跃 ROB 表项；由于
一个表项可折叠最多三条顺序指令，不能把它改写成“最多只有 64 条 ISA 指令在途”。

### 9.2 ROB：64 项瘦表项（"只管序，不管值"）

C910 的 ROB 有 64 个 40 位物理表项。满足条件的相邻指令可折叠进同一项，并用组内
完成计数跟踪，因此理想表示能力可接近 192 条指令；实际折叠率受指令类型、异常、控制流
和其他资源限制。与保存完整结果和 PC 的教科书式 ROB 相比，当前表项把多类状态放在外部：

| 教科书 ROB 存 | C910 放哪 |
|---------------|-----------|
| 完整 PC | 不存，只存 3 位 pc_offset，退休时增量重建；跳转目标在 IU 的 PCFIFO |
| 结果数据 | PRF（物理寄存器堆，写回即入） |
| 异常向量 / mtval | 单独 1 项异常缓冲 |

**ROB 只管"序"，不管"值"——这是 RTU 全部设计的纲。**

- **退休 PC 增量重建**：维护一个退休游标 `rob_cur_pc`，非跳转项退休 `cur_pc += pc_offset`，
  跳转项 `cur_pc = PCFIFO 弹出的目标`。**少量固定的退休侧加法逻辑替代了
  每个 ROB 表项各存一份 39 位 PC**——这就是
  ROB 敢不存 PC 的底气。
- **指令折叠**：一项最多装 3 条相邻简单指令，`cmplt_cnt[1:0]` 跟踪剩余完成数。退休口
  3 项/拍 × 3 条/项 = 峰值 9 条/拍。
- **完成端口集中译码后按位分发**：7 条管线的 IID 低 6 位分别经
  `ct_rtu_expand_64` 做 6-to-64 二进制到独热译码，再把第 N 位送给 entryN。
  表项侧因此只需按位接收完成事件，但顶层译码逻辑真实存在，不能称为“零译码”。

### 9.3 异常单项擂台 + flush 状态机

**精确异常只需要"最老的未退休异常"**，所以异常现场（70 位）只存**一项**。4 条能报
异常的管线（IU pipe0、BJU、LSU pipe3/4）每次异常完成都来"挑战擂主"：10 个 compare_iid
两两比较，比现任老才换人。误预测、LSU 顺序违例、vsetvl flush **复用同一套擂台**——
都需要"最老者退休时触发恢复"的语义。

**flush 状态机**是全核恢复的总指挥：
```
触发A 异常/中断/CSR副作用 → FLUSH_FE（前端要转向 trap 入口）
触发B 误预测分支退休       → FLUSH_IS（IFU 已被 BJU 重定向过，只清 IS/RF，更轻）

IDLE → FLUSH_FE/IS → [WF_EMPTY 等待] → FLUSH_BE → IDLE
```
RTL 信号名 `retire_flush_pipeline_empty` 的直接条件是
`pst_retired_reg_wb && lsu_all_commit_data_vld`。它只确认恢复时必须保留的已退休
寄存器版本已经写回、LSU 已提交数据达到要求，不是逐级检查全核流水线为空。
这些必须保留的结果尚未落定时不能进入 FLUSH_BE，体现的是“先稳定提交态，再恢复推测态”。
FLUSH_BE 发出后端恢复脉冲 **`rtu_yy_xx_flush`**。整数/浮点重命名表从退休态
映射恢复，发射队列和各执行单元只清除各自 RTL 明确标记为推测态的状态；已经退休的
PST 映射和已经提交、必须继续完成的 store 等状态不会被一并清除。

### 9.4 PST：物理寄存器生命周期与回收

PST（Physical register Status Table）管理 preg 的分配与回收。整数物理编号
共有 96 个，其中 preg0 固定为 x0 的已退休、已写回映射；preg1~95 才各有一个
5 态状态机：
```
DEALLOC(空闲) → WF_ALLOC(预取) → ALLOC(已派给指令,推测) → RETIRE(生产者已退休,是某逻辑寄存器正身)
            → RELEASE(下任写者退休,等写回确认) → DEALLOC
```
三条铁律：
1. **flush 时 ALLOC/WF_ALLOC 回 DEALLOC，RETIRE 保持**。每个 RETIRE 项输出逻辑目的
   位图，flush 时 RAT 并行写入这些已提交映射；该局部恢复不需要 walk ROB 或在 RAT 内
   保存每分支 checkpoint，但需要 PST 状态、位图转置、编码网络和宽恢复总线。
2. **旧 preg 在"下一任写者退休"时才释放**——每项记着前任 preg（rel_preg），保证旧值
   一直可读到新映射安全提交。
3. **没写回不回收**——否则新主人可能读到撕裂值。

**重命名恢复的三条路线（理解 C910 RTU 的关键，也是乱序核的经典考点）**：误预测/异常后，
架构 RAT（逻辑→物理映射）被推测指令污染了，必须恢复到"最后一条已退休指令"的映射。业界三招：

| 方法 | 怎么做 | 代价 |
|------|--------|------|
| **Checkpoint 快照**（Alpha 21264） | 每条分支处存一份完整 RAT 快照，恢复即回滚 | 快，但每个快照要一整份 RAT 存储，数量有限——分支密集时快照不够用 |
| **ROB-walk 倒带** | 从 ROB 尾逐条撤销重命名直到出错点 | 省存储，但**慢**（要走很多拍） |
| **C910 退休态位图重建** | PST 从 RETIRE 项生成完整已提交映射，RAT 并行写入 | 不 walk ROB，也不在 RAT 内保存每分支快照；需要 PST 状态、宽恢复总线和编码网络 |

整数和浮点 PST 的 RETIRE 状态记录最后已提交映射。每个有效物理项输出其逻辑目的
位图，转置和编码网络把这些位图重新构造成 32 个架构寄存器的物理编号；
`rtu_yy_xx_flush` 到达时，整数 RAT 通过 224 位总线、浮点 RAT 通过 192 位总线并行
恢复。局部映射写入可在一个恢复边沿完成，但端到端恢复还包括 flush 仲裁、前后端清空、
已退休未写回状态等待和重新取指，不能把它概括成“误预测总惩罚只有一拍”。当前向量
VRT/PST 为 dummy 边界，不应把整数/浮点恢复机制无条件推广到完整 RVV 状态。

> 细节见 [RTU 文档索引](./rtu/README.md)（00 至 07 共 8 篇）。

---

## 9.5 流水线 Stall 条件系统化

这是 IPC 分析和瓶颈定位的核心知识。C910 的 stall 分布在两个顺序段（前端/后端
顺序转换点），乱序段本身不 stall（发射队列里等着就行）。

### 前端 stall（ID 级停流 → IFU 停取）

| Stall 原因 | 信号 | RTL 位置 | 常见触发场景 |
|-----------|------|---------|------------|
| **IR 级误预测等待** | `iu_idu_mispred_stall` | ct_idu_ir_ctrl.v | BJU EX1 发现误预测，ID/IR 等新指令到来 |
| **RTU flush 等待** | `rtu_idu_flush_stall` | ct_idu_ir_ctrl.v | 精确异常/分支退休 flush 期间，IR 禁止放行 |
| **整数 preg 耗尽** | `ctrl_ir_preg_stall` | ct_idu_ir_ctrl.v:1024 | 96 个物理编号中没有满足分配条件的整数目的项；不等同于固定 64 条整数指令在途 |
| **浮点 freg 分配失败** | `ctrl_ir_freg_stall` | ct_idu_ir_ctrl.v:1036 | 当前 64 项浮点物理池没有满足本拍目的分配的项 |
| **向量 vreg 分配失败** | `ctrl_ir_vreg_stall` | ct_idu_ir_ctrl.v:1030 | 接口保留；当前 VRT/向量 PST 为 dummy，需先确认该 stall 路径是否可达 |
| **ereg 耗尽** | `ctrl_ir_ereg_stall` | ct_idu_ir_ctrl.v:1042 | vsetvl 配置重命名耗尽 |
| **fence 屏障** | `fence_ctrl_id_stall` | ct_idu_id_ctrl.v | fence 到达 ID 级，等 LSU 排空 |
| **复杂指令拆分** | `ctrl_split_long_id_stall` | ct_idu_id_ctrl.v | 复杂指令（如向量）拆多微操，下一拍 ID 继续完成拆分 |

**关键规律**：`ctrl_ir_stage_stall = preg_stall | vreg_stall | freg_stall | ereg_stall | flush_stall | mispred_stall`。
IR 级是"乱序窗口的门"——任何物理寄存器分配失败都在这里阻塞，而**不会等到发射**。

### 后端 dispatch stall（IS 级停派遣）

| Stall 原因 | 信号 | 触发条件 |
|-----------|------|---------|
| **ROB 满** | `ctrl_is_rob_full` | 64 个 ROB 物理项无法接收下一组；已表示的 ISA 指令数取决于每项折叠数 |
| **任一发射队列满** | `ctrl_is_iq_full` | AIQ0/AIQ1/BIQ/LSIQ/SDIQ/VIQ0/VIQ1 任意一个满 8 项且有新指令派遣该队列 |
| **VMB 满** | `ctrl_is_vmb_full` | 向量内存缓冲满（向量 load/store 特有） |

等式：`ctrl_is_dis_stall = ctrl_is_rob_full | ctrl_is_iq_full | ctrl_is_vmb_full`

**分布式发射队列的 stall 特性**：如果只有 FDIV 在飞（pipe0 ALU 同时闲着），
AIQ0 可能满（8 条 DIV 排队），但 AIQ1 空着——这时派遣 add 指令到 AIQ1 没问题，
**派遣 div 就卡**。这是分布式队列"独立满"的典型表现。

### 访存相关 stall

| Stall 来源 | 信号 | 触发 |
|-----------|------|------|
| **LQ 满** | `lsu_idu_lq_full` | Load 队列 16 项全满，新 load 无法进入 LSIQ |
| **SQ 满** | `lsu_idu_sq_full` | Store 队列 12 项满，新 store-addr 无法派遣 |
| **RB 满** | `lsu_idu_rb_full` | miss 请求缓冲 8 项满，新 miss 无法登记 |
| **TLB busy** | `lsu_idu_tlb_busy` | TLB miss 期间 PTW 占用管线，LSIQ 发射项卡在 DC 级 |
| **fence/sync 等待** | `lsu_idu_wait_fence[11:0]` | 对相应 LSIQ 项记录排序等待；完成条件依 fence 类型和页面属性而异 |
| **第二段/重发标记** | `lsu_idu_secd[11:0]` | 直接连自 `ld_da_idu_secd`，在 LSIQ 内设置 `unalign_2nd`；不能笼统等同所有 spec-fail/discard |

### stall 瓶颈定位方法（PMU 事件）

```
高 preg_stall   → 整数目的物理项回收跟不上分配；再查 ROB 头、整数生产者和 load miss
高 rob_full     → ROB 满：同上，或者退休端 flush 太频（误预测/异常）
高 iq_full (AIQ0)  → pipe0 类整数操作或整数 DIV 入口拥塞；AIQ1 则查 MULT/pipe1
高 iq_full (LSIQ)  → 访存密集 + miss 多（load 慢返回）
高 fence_stall  → 结合 event41、WMB/RB 和具体 fence 类型确认排序等待
高 Spec Fail    → 顺序违例频繁；考虑 store 优先、数据相关分析
```

---

## 10. 多核与缓存一致性

### 10.1 系统拓扑

产品论文描述每簇最多 4 核、共享 L2 并采用 MOESI 一致性；当前仓库的生成顶层则实际
连接 core0/core1，两者是不同层次的口径。RTL 中每核 BIU 提供 ACE 风格读、写和
snoop 通道，CIU 通过 PIU、SNB/SAB、CTCQ、VB、NCQ 与 L2/外部接口组织一致性事务。
CIU 能根据 L2 返回的 presence/filter 元数据选择侦听目标，也能通过 CTCQ 接收被侦听
核心返回的数据。由此可以证明“存在多核一致性请求、定向侦听和核间数据返回路径”，
但不能只凭模块名把每条事务的协议状态变化补成一张未经 RTL 验证的 MOESI 转移表。

| 项目 | 产品资料口径 | 当前生成 RTL 可直接确认 |
|------|--------------|--------------------------|
| 多核规模 | 每簇最多 4 核 | 顶层 core0/core1 有效；CIU/L2 元数据保留 4-bit 参与者字段 |
| 一致性协议 | MOESI | ACE 风格事务、snoop、响应、presence/filter 更新；没有集中保存的显式五态枚举寄存器 |
| 一致性互联 | 共享 L2 + snoop filter | CIU 的 PIU、SNB/SAB、CTCQ、VB、NCQ 和 L2/EBIU 接口 |
| 核间数据 | 支持 cache-to-cache transfer | snoop CD 数据可进入 CTCQ 并继续送往请求事务 |
| 原子访问 | LR/SC + AMO | LSU local monitor、队列和一致性事务共同维护；见 §7.5 |

### 10.2 产品 MOESI 口径与当前 RTL 可证明的状态边界

L2 每一路保存的压缩状态字段来自 `ct_l2c_tag.v` 与 `ct_l2c_cmp.v`：

```
status_set_din[7:0] = { cp[3:0],  valid,  shared,  dirty,  pend }
                        ████████   █       █        █       █
                        presence   有效    共享属性  脏      待定
```

`valid`、`shared`、`dirty` 和 `pend` 分别记录该 L2 路是否有效、RTL 所称的
shared/unique 属性、脏状态和正在处理的占位状态；`cp[3:0]` 由 CIU 请求置位或清位，
命中时返回 CIU，作为选择 PIU snoop 目标的 presence/filter 线索。当前集成只有
core0/core1 有效，因此 4-bit 字段不能直接解释为“当前一定有四个核”。

这些位可以表达类似 MESI/MOESI 状态判断所需的部分事实，却不是
`M/O/E/S/I` 的独立枚举。特别是 `dirty=1 && shared=1` 不能脱离事务响应、数据责任者、
CIU SAB 状态和核内 Cache 状态而直接命名为 Owned；RTL 也没有在 L2 状态中保存独立
owner 编号。MOESI 仍是理解产品目标的有用模型：Owned 概念允许一个脏数据责任者与
其他共享副本并存，避免为每次共享读取先回写主存。但对当前开放 RTL，严谨分析应追踪
ARSNOOP/AWSNOOP、AC/CR/CD 响应、SAB 状态、CTCQ 数据、L2 `setcp/clrcp` 与
`shared/dirty/pend` 的实际变化，再判断具体事务是否实现了对应语义。

### 10.3 CIU 架构与 SNB 侦听总线

CIU 内部关键子模块：

```
         核0 PIU ──┐
         核1 PIU ──┤        ┌── L2 Cache bank 0/1
                   SNB ─────┤
         外部 EBIU ─┤        └── 外存 AXI4
         （共 4 个 PIU 接口 + 1 个外部接口）
         snoop filter (SAB): 每项追踪 1 个地址的状态 + cp 位图
         CTCQ: cache-to-cache 数据传输队列
         VB:   CIU 侧逐出缓冲
         NCQ:  non-cacheable 请求队列
```

**一次跨核一致性读的可验证流程**可以按事务接口分解：

```
① core1 发 AXI AR 请求（ARSNOOP=ReadShared/ReadUnique）到 CIU
② CIU 查 snoop filter → cp[0]=1（core0 有副本）→ 只侦听 core0
   （如 sf_dis=1 则降级为广播所有核）
③ SNB 向 core0 发 AC（snoop address）请求
④ core0 LSU 的 SNQ（6 项）接收 → 借 load 管线读 D-Cache tag
   → 根据命中、脏/共享属性和 snoop 类型形成响应
⑤ core0 经 CR 返回响应；需要提供数据时，经 CD 把数据送入 CTCQ
⑥ CIU 将 snoop 响应、L2/外部结果和请求状态合并，向 core1 完成响应
⑦ CIU/L2 按事务结果更新 setcp/clrcp、shared、dirty、pend 等元数据
```

这条链能证明定向侦听和 cache-to-cache 数据路径存在，但单凭 `cp` 命中或 CTCQ
有数据，不能断言本次一定绕过 DDR，也不能直接命名为 `M→S`、`I→S` 或
`dirty&&shared→O`。要给出产品 MOESI 状态转移，还必须联合 ARSNOOP、CRRESP、
CD 数据来源、L2 命中/外部访问和核内 line 状态。

**SNQ 与本核在飞操作的互锁**：
- SNQ 到来时，若 LFB 正在 refill 同一地址 → 等 refill 完再侦听
- WMB 有同地址写在飞 → 先让 WMB 写完（内存可见），再侦听
- LQ 中已完成的 load 若与侦听地址重叠 → **触发 RAR 违例**（LQ 检测），
  该 load 之后的依赖指令需重执行（这是多核下 RAR 违例的根本来源）

### 10.4 一致性请求类型（ARSNOOP/AWSNOOP 编码）

CIU 通过 AXI ACE（AXI Coherency Extensions）总线与核通信：

| 请求类型 | ARSNOOP | 含义 | 典型触发 |
|---------|---------|------|---------|
| ReadShared | `0001` | 读，允许共享副本 | 普通 load miss |
| ReadUnique | `0111` | 读并独占，其他核无效化 | 写前需读的 store miss |
| CleanUnique | `1011` | 独占但不取数据（已有副本） | AMO 取独占权 |
| MakeUnique | `1100` | 仅驱逐其他核副本 | 整行 store（写分配优化） |
| WriteBack | （AW） | 脏行写回 | M→I 替换 |
| Evict | （AW） | 干净行失效通知 | E/S→I 替换，维护 snoop filter 准确 |

**Evict（驱逐通知）是 snoop filter 准确性的代价**：即使替换干净行（S/E→I），
也必须发 Evict 通知 CIU 更新 cp 位图。否则 snoop filter 以为该核还有副本，
会发出不必要的侦听（伪侦听，浪费 SNQ 和管线资源）。

### 10.5 多核常见性能陷阱

| 现象 | 根本原因 | 微架构对应 |
|------|---------|-----------|
| 锁竞争下 load 命中率下降 | 同一行在多个核之间反复取得共享/独占权限，可能伴随 false sharing | L1 D-Cache miss、AC/CR/CD 与 CTCQ 活动增加 |
| AMO/LR-SC 严重变慢 | 多核竞争同地址，lm_state 反复 WAIT_REQ→IDLE（sc 失败重试） | 见 PMU lm_fail 事件 |
| fence 串行化 | 高频 fence 排空所有队列，stop-the-world | Sync Stall 计数爆高 |
| RAR 违例突然增多 | 多线程访问同一 cache 行，snoop 导致 LQ-RAR | LSU Spec Fail 计数异常 |

### 10.6 BIU：每核 AXI/ACE 主端口（RTL: `biu/rtl/ct_biu_*`）

前面反复出现"发 BIU 请求""BIU AXI 读"，这里交代 BIU 到底是什么。**完整的访存外出路径**是：
```
核内 (IFU refill / LSU RB / PTW)  →  BIU (每核 ACE 主端口)  →  CIU (一致性互联+侦听过滤)
                                      →  共享 L2  →  外部 AXI4  →  DDR
```
**BIU 是每个核唯一的对外端口，CIU 是把多个核的 BIU 汇到一起做一致性的互联**——分工清晰：
BIU 管"本核怎么上总线"，CIU 管"多核之间怎么保持一致"（§10.3）。

**协议与位宽**：ACE（AXI Coherency Extensions），数据 **128 bit**、地址 40 bit、
AXI ID **5 bit**。通道齐全：读 AR/R、写 AW/W/B、**侦听 AC/CR/CD**（ACE 一致性三通道）。

| 子模块 | 职责 | 关键数字 |
|--------|------|----------|
| **read_channel** | 读请求上 AR、读数据经 R 返回 IFU/LSU | AR/R 各两项边界槽；AXI ID 组合分类 IFU 两类和 LSU linefill 类，BIU 本地不保存完整 outstanding 表 |
| **write_channel** | store 写与脏行逐出 | 12 项队列只保存 store/victim 来源；两类 AW/W 必须配对，固定选择关系为 victim 优先 |
| **snoop_channel** | 外部侦听 AC 进、CR/CD 出 | AC/CR/CD 各双缓冲；转发给 LSU 的 SNQ（§10.3） |
| **req_arbiter** | 读总线仲裁 | 固定选择关系为 LSU `dp_req` 屏蔽 IFU，并在上游给 IFU 请求拼接专用 AXI ID |
| **csr_req_arbiter** | 非缓存/设备(IO)访问路由 | NC/强序请求单独走；**CP0 > HPCP** 优先 |
| **lowpower** | 配合 WFI | 每通道独立时钟门控；读写都空闲时拉 `biu_yy_xx_no_op`，配合 `cp0_biu_lpmd_b` 静默总线 |
| **other_io_sync** | 跨时钟域同步 | 把系统侧的 6 类中断（me/mt/ms/se/st/ss）+ 调试请求两级触发器同步到核时钟 |

store 与 victim 使用不同的边界缓冲和来源状态，固定优先选择 victim；这可以从 RTL
确认，但性能动机和对 store 饥饿的影响需要运行数据验证。读返回按 RID 分类给 IFU 或
LSU，ID 是来源名牌，不是 BIU 内部完整的事务重排表。端到端并发上限由 IFU/IPB、LSU
RB/LFB、总线 ID/信用、CIU/SNB 和下层资源共同决定，不能由 BIU 两项槽或 RB 八项中的
任何单个数字直接给出。

**两个值得带走的通用思想**：
- **AXI ID = 乱序事务的"名牌"**。总线允许多笔读/写同时在飞、且**乱序返回**，怎么知道
  回来的数据是谁的？给每笔请求贴一个 ID，返回时认牌对号入座。这与 ROB 用 IID 管乱序指令
  （§9.1）、cache 用 MSHR/RB 管乱序 miss 是**同一个方法**——*用一个小标签把"并发 + 乱序"
  管起来*。BIU 把 IFU/LSU 的 miss 项编成 AXI ID，数据回来按 `rid` 找回主人。
- **ACE 让 BIU 一个端口演两个角色**：对本核自己的访存，BIU 是**主**（主动发 AR/AW 取数）；
  对别核发来的侦听，BIU 又是**从**（被动收 AC、回 CR/CD 交数）。"既要主动取、又要被动交"
  正是缓存一致性的本质——你的 cache 既服务自己，也要应别人之需（呼应 §10.3 的 SNB 侦听）。

---

## 11. 中断与异常处理（trap / CLINT / PLIC）

§9 讲了 RTU 如何在退休级**挑出最老的异常并 flush**；本节接着讲**陷入之后发生了什么**
——控制状态寄存器（CSR）怎么被原子更新、执行流怎么跳到处理程序。这是精确异常对软件
可见的那一面（H&P 附录「异常与中断」）。

### 11.1 统一陷入入口（trap entry，RTL: `cp0/rtl/ct_cp0_regs.v`）

**特权级**：M / S / U 三级，当前级由 `pm[1:0]` 跟踪（`2'b11`=M、`2'b01`=S、`2'b00`=U），
复位进 M 态。完整 trap CSR 一应俱全：

| 组 | CSR | 作用 |
|----|-----|------|
| 状态 | `mstatus`/`sstatus` | MIE/MPIE/MPP/SPP 等中断使能与特权栈 |
| 向量 | `mtvec`/`stvec` | trap 入口基址 + 模式（Direct / Vectored） |
| 返回 PC | `mepc`/`sepc` | 出错/返回指令地址 |
| 原因 | `mcause`/`scause` | bit63=中断标志，低位=原因码 |
| 出错值 | `mtval`/`stval` | 出错地址或指令编码 |
| 使能/挂起 | `mie`/`mip`、`sie`/`sip` | 各类中断使能与挂起位 |
| 委派 | `medeleg`/`mideleg` | 把异常/中断下放给 S 态处理 |
| 翻译 | `satp` | Sv39 页表基址 + ASID |
| **玄铁私有** | `mxstatus`/`mhcr`/`mcor`/`mhint`/`mrvbr`/`mcer` | 扩展状态、cache/预取硬件控制、复位向量基址、cache 错误记录 |

**陷入时一拍内原子完成的动作**（RTU 给出 `rtu_cp0_expt_vld` + epc + vec + mtval 那拍）：
```
若不委派（去 M 态）:  mepc←出错PC ; mcause←{int,code} ; mtval←出错值
                      mstatus: MPP←pm ; MPIE←MIE ; MIE←0   （特权与中断使能压栈）
                      pm←M
若委派（去 S 态）:    sepc/scause/stval 同理 ; SPP←pm[0] ; SPIE←SIE ; SIE←0 ; pm←S
```
**返回操作要按目标特权级区分**：`mret` 恢复 `MIE←MPIE`、`pm←MPP`，并按规范
更新 MPIE/MPP，PC 回到 `mepc`；`sret` 恢复的是 `SIE←SPIE`、`pm←SPP`，并更新
SPIE/SPP，PC 回到 `sepc`。二者采用同一类“先前使能 + 先前特权级”栈式思想，
但字段和返回 PC 不同，不能把 `sret` 写成读取 MPP/mepc。

**trap 入口 PC 怎么算**（mtvec 两模式）：
- **Direct（mode=0）**：所有 trap → `base`；
- **Vectored（mode=1）**：中断 → `base + 4×cause`（按号散开，省去软件分派），异常仍 → `base`。

**委派（delegation）**：在 S/U 态发生、且 `medeleg/mideleg` 对应位置 1 时，trap 直接交
S 态，**不绕一圈 M 态**——这是跑 Linux 时绝大多数缺页/系统调用的快路径。

**异常原因码**（RISC-V 标准，`mcause` 低位）：
| code | 异常 | code | 异常 |
|------|------|------|------|
| 1 | 取指访问错 | 8/9/11 | U/S/M 态 ecall |
| 2 | 非法指令 | 12 | 取指缺页 |
| 3 | 断点 | 13 | load 缺页 |
| 4/6 | load/store 地址未对齐 | 15 | store 缺页 |
| 5/7 | load/store 访问错 | — | — |

**中断原因码**（`mcause` 最高位=1 时）与对应挂起/使能位（mip/mie）：

| code | 中断 | 挂起/使能位 | 来源 |
|------|------|-------------|------|
| 1 / 3 | S / M 软件中断 | (s/m)sip · (s/m)sie | CLINT `msip`（§11.2，核间 IPI） |
| 5 / 7 | S / M 定时中断 | (s/m)tip · (s/m)tie | CLINT `mtimecmp`/`stimecmp`（§11.2） |
| 9 / 11 | S / M 外部中断 | (s/m)eip · (s/m)eie | PLIC（§11.3） |
| 玄铁扩展 | **计数器溢出中断** | `moie` | PMU/HPCP 计数器溢出（§13.2，性能采样） |

C910 实现这三类标准中断 + 玄铁的 **moie 计数器溢出中断**。中断须同时满足"mip 挂起 +
mie 使能 + 当前特权级允许"，才在退休边界被接受。

**两条铁律**：①**中断只在退休 inst0 边界被接受**（保证精确）；②**同一指令上中断优先于异常**
（`expt_vec[5]` 中断标志置位即走中断向量）。**无 NMI、无 CLIC**——采用经典 CLINT+PLIC 模型。

### 11.2 CLINT：核内本地中断（RTL: `clint/rtl/ct_clint_func.v`）

**先理解"为什么中断要分两套系统"**——这是中断体系最重要的思想。中断按"是否需要全局仲裁"
天然分两类：**本地中断**（定时器、核间软件中断）每核私有、延迟敏感、不需要在多个核/多个源
之间排队，交给紧贴核的 **CLINT**；**平台中断**（外设：网卡、串口、DMA…）全局共享、数量多、
要按优先级仲裁分发，交给 **PLIC**（§11.3）。**把"私有快路径"和"共享需仲裁"分开做**，是
中断系统的经典二分，也是 RISC-V CLINT/PLIC 划分的根本动机。

CLINT 管两类本地中断，服务 **2 个 hart**：

**① 定时器中断——"比较寄存器式定时器"的通用方法**：有一个 64 位全局自增计数器 `mtime`
（所有核看同一时间基准），外加每 hart 一个 `mtimecmp`。软件往 `mtimecmp` 写一个"未来的
时间值"，当 `mtime ≥ mtimecmp` 时硬件拉高 `mtip`。**要下一次中断，就把 mtimecmp 再加一个
周期**——OS 的时间片调度、超时、jiffies 全靠它。C910 还额外提供 S 态的 `stimecmp`：传统上
S 态（OS）想要定时中断，得陷入 M 态求固件（SBI）代写 mtimecmp，每次都有特权切换开销；
直接给 S 态自己的比较器后，**OS 不绕 M 态自管定时**（即 Sstc 思想）——这是"把高频快路径
下放到低特权级"的典型优化。

**② 软件中断（IPI）——多核协作的基础原语**：每 hart 一个 `msip` 位，**一个核写另一个核的
msip，就能"戳"对方产生一次中断**。这就是 IPI（核间中断），是 SMP 一切跨核协作的底层信号：
启动时 boot hart 用它唤醒自旋等待的 secondary hart（§11.5）；运行时用于 **TLB shootdown**
（改了页表通知别核刷 TLB）、调度迁移、panic 广播。**"写一个内存映射位 → 对方收到中断"**
是用最小硬件实现核间通信的经典手法。

### 11.3 PLIC：平台级中断（RTL: `plic/rtl/plic_*`）

PLIC 仲裁平台级中断。当前 `cpu_cfig.h` 定义 144 路外部 pad 中断；
`openC910.v` 以 `INT_NUM=PLIC_INT_NUM+16=160` 例化 PLIC，其中附加 16 个位置包括
固定保留位和 L2 ECC 中断接入口。当前只启用 core0/core1，因此 `HART_NUM=2`；
PLIC RTL 和配置宏保留更大源数、最多四 hart 的可配置结构，但不能把模板上限当成
当前有效集成规模。

| 维度 | 规格 |
|------|------|
| 中断源 | 144 路外部输入；PLIC 内部总线 160 位（另 16 位含保留/L2 ECC 接入口） |
| 优先级 | 每源 **5 bit = 32 级**（0=关闭） |
| context | 当前 2 hart，每 hart 有 M/S 阈值、使能与 claim 路径；模板可扩到 4 hart |
| 阈值 | 每 context 一个 priority threshold，优先级须**严格大于**阈值才上报 |
| 认领/完成 | claim 读出最高优先 ID 并进入 active 处理；complete 写回 ID 结束 active，持续有效的电平源可再次挂起 |
| 仲裁结构 | `plic_32to1_arb` 分轮归约优先级和 ID；内部实例保留 1024 槽仲裁宽度，当前有效请求仅来自已接入的 160 位源总线 |
| 总线 | APB（`csky_apb_1tox_matrix`） |

**claim/complete 握手是 PLIC 的精髓**：硬件做完优先级仲裁，软件用一次读（claim）拿到
当前最高优先中断号、一次写（complete）结束该 active 处理状态；电平源若仍保持有效，
随后仍可能再次挂起。这个握手把“谁最该被处理”的复杂仲裁
留给硬件、把控制权交给软件，是中断控制器的经典分工（对比 ARM GIC 的 IAR/EOIR）。

### 11.4 中断/异常设计取舍小结

| 设计点 | C910 选择 | 动机 | 教科书坐标 |
|--------|-----------|------|-----------|
| 中断模型 | 经典 CLINT+PLIC，无 CLIC/NMI | 跑 Linux 够用、逻辑简单 | RISC-V 特权规范 |
| trap 入口 | Direct/Vectored 两模式 | 向量模式省软件分派 | 异常向量化 |
| 委派 | medeleg/mideleg 直达 S 态 | 缺页/syscall 不绕 M 态 | 虚拟化/OS 快路径 |
| 中断时机 | 仅退休 inst0 边界 | 保证精确异常 | Smith&Sohi 精确中断 |
| S 态定时器 | CLINT 直供 stimecmp | 免 M 态转发定时中断 | Sstc 思想 |
| PLIC 仲裁 | 硬件层次树 + 软件 claim/complete | 硬件挑最优、软件掌控 | GIC 式分工 |

### 11.5 复位与启动流程

把"陷入"反过来看就是"上电启动"——核如何从复位进入第一条指令：

| 复位后状态 | 值 | 来源 |
|------------|-----|------|
| 特权级 | **M 态**（pm=2'b11） | `ct_cp0_regs.v` 复位值 |
| 起始 PC | **复位向量基址 MRVBR** | `mrvbr_reg ← biu_cp0_rvba`（来自外部引脚 `pad_core_rvba`，每核可不同） |
| 中断 | 全关（MIE=0） | mstatus 复位 |
| 地址翻译 | 关（satp=0，裸机物理寻址） | satp 复位；属性走 sysmap PMA（§8.5） |
| Cache | 全 invalid（需预热） | tag valid 位复位清零 |
| 核 ID | 每核唯一 hartid | `biu_cp0_coreid ← pad_core_hartid[2:0]` |

**典型启动序列**：复位 → M 态从 MRVBR 取第一条指令 → 初始化（配 PMP 划区、建页表、
开 cache、设 mtvec）→ 必要时 `mret` 降到 S 态跳入 OS。**多核 SMP**：各核同时从各自
MRVBR 启动，通常约定一个 boot hart 做初始化、其余 secondary hart 自旋等 IPI（msip）
唤醒——这正是 §11.2 CLINT 软件中断的用武之地。

---

## 12. 低功耗与时钟门控

高性能核的功耗大头是**时钟翻转的动态功耗**。C910 的省功耗哲学贯穿全文，这里系统收口
（H&P 第 1 章「功耗墙」、附录「能效」）。

### 12.1 细粒度三级时钟门控（RTL: `clk/rtl/gated_clk_cell.v`）

大量局部时序逻辑通过 ICG（集成时钟门控）单元 `gated_clk_cell` 获得门控时钟，其
典型使能式为：
```
clk_en = ( global_en && (module_en || local_en) ) || external_en
         ▲全局总开关        ▲模块级    ▲局部细粒度    ▲旁路(扫描/特殊)
```
这组输入把全局使能、模块使能、局部活动和扫描/特殊旁路组合起来，使模块可以只在需要
更新状态时打开局部时钟。按当前 `gen_rtl` 中
`gated_clk_cell <instance>(...)` 的实例语句统计，共有 **748 个实例，分布在
240 个 Verilog 文件**。这说明门控覆盖很广，但不能写成“所有时序逻辑都经过 ICG”：
复位、跨域同步和少数常开控制仍直接使用 `forever_cpuclk`。`*_gateclk_vld`
表示对应局部状态有更新需求；只有结合所在门控单元的全局、模块和外部使能，才能判断
该门控时钟在这一拍是否真正翻转。

### 12.2 全部省功耗手法汇总

C910 的低功耗手法分两大类——**硬件自动**做的，和**软件可控开关**的。

**(A) 自动硬件手法**（无需软件干预，时刻在省）：

| 手法 | 所在 | 省什么 |
|------|------|--------|
| **细粒度时钟门控** | §12.1 | 当前生成 RTL 中 748 个 `gated_clk_cell` 实例，广泛覆盖表项、队列和流水级局部状态 |
| **way-predict** | §4.3 / §8.2 | 只读预测命中那一路 SRAM，省约一半 I-Cache Data 读功耗 |
| **per-bank 精确激活** | §4.3 / §8.2 | 只唤醒目标 bank 的 SRAM（CEN 精确使能） |
| **Loop Buffer** | §4.2 | ACTIVE 时由本地 Entry 阵列向 IBDP 重放短循环，当前迭代的功能供指不依赖重新取得 I-Cache 数据；BHT 等循环控制信息仍参与 |
| **变延迟乘除** | §6.2 / §6.5 | DIV/FDIV 按操作数有效位宽迭代（`skip_srt`/提前结束）——小操作数少迭代、少翻转 |
| **BIU 通道门控** | §10.6 | 读/写/侦听每通道独立门控；读写全空拉 `no_op` 静默总线 |
| **L2 可编程延迟** | §8.4 | 配低速低功耗 SRAM |
| **预取机会化丢弃** | §7.4 PFU | 预取资源紧张直接放弃，不硬撑 |

**(B) 软件可控开关**（mhcr / mhint CSR）——**常被忽视的一大类:几乎每个耗电的推测功能
都能被软件单独关闭**，在功耗敏感、或分支/访存行为规律差（预测净亏）的代码段省动态功耗：

| CSR | 可逐项开关的功能 |
|-----|------------------|
| **mhcr** | I-Cache(ie) · D-Cache(de) · 写分配(wa) · 写回(wb) · **RAS(rse)** · **BHT 方向预测(bpe)** · **BTB(btbe)** · **间接预测(ibpe)** · **L0-BTB(l0btbe)** |
| **mhint** | **D/I-Cache 预取(dcache/icache_pref_en)** · **L2 预取(l2pld)** · **way-predict(iwpe)** · **loop(lpe)** · 预取距离 · ECC 使能 |

> **关键认识**：分支预测器、预取器、way-predict 这些"赌一把省时间"的结构**自身要耗电**
> （每拍读 SRAM、更新状态）。当代码预测命中率低时，开着它们反而净亏——所以 C910 把它们
> 做成 **可逐项关断**，让软件按场景权衡"预测收益 vs 预测功耗"。这是教科书很少提、但商用核
> 必备的一层低功耗手段。

### 12.3 WFI 低功耗模式（RTL: `cp0/rtl/ct_cp0_lpmd.v`）

`wfi` 指令进入 LPMD（low-power mode）状态机（`ct_cp0_lpmd.v`，**3 态**）：执行到
`inst_lpmd_ex1_ex2` → **IDLE → WAIT**（向 BIU 发请求、等总线排空 `lpmd_ack`）**→ LPMD**
（停掉核心时钟）→ 输出 `cp0_biu_lpmd_b` / `cp0_had_lpmd_b` 告知 BIU 与调试模块；
**任一使能中断 `regs_lpmd_int_vld` 到来即唤醒**回 IDLE。这是"无事可做时整核休眠、
来活立刻醒"的标准 WFI 实现——注意进 LPMD 前必须先等 BIU **ack**，确保在飞的总线事务
落定、不丢数据。

> **低功耗范围说明（重要）**：开源 C910 RTL 里**没有**显式的电源门控（power gating）、
> 状态保持（retention）或电压域信号——核内低功耗手段是 **细粒度时钟门控（§12.1，748 个
> ICG）+ WFI 整核时钟停** 两层。真正的"断电休眠"（power domain 切断、DVFS 调压调频）
> 属 SoC 集成层，由片外电源管理实现，不在核 RTL 范围内。换句话说：**C910 核做的是
> "省着翻转时钟"，不是"断电"**。

**取舍**：细粒度门控省动态功耗显著，但每个 ICG 有面积/布线代价、且门控使能在关键路径上
要提前一拍算好；激进的整核休眠省得多但有唤醒延迟。C910 选择"门控做到极细 + WFI 整核
休眠"两手都要——这是电池/散热受限的应用级核的典型选择。

---

## 13. 物理内存保护 PMP 与 可观测/调试（PMU / HAD）

### 13.1 PMP 物理内存保护（RTL: `pmp/rtl/ct_pmp_*`）

**先理解 PMP 解决什么问题**：MMU 页表管的是"虚拟地址能不能访问"，但有两类场景页表管不了——
①机器态裸跑（MMU 关闭，无页表）；②要防止连 S 态（OS 内核）都不能碰的区域（如安全固件、
TEE 飞地）。PMP 在**物理地址**这一层、由 **M 态**设置一组"谁能访问哪段物理内存"的规则，
是隔离与可信执行（TEE）的硬件地基（RISC-V 特权规范 PMP 章）。**思想**：MMU 是"翻译 + 权限"，
PMP 是翻译之后再加一道"物理层闸门"——纵深防御。

| 项目 | C910 |
|------|------|
| 区域数 | **8 个**（pmpaddr0~7，cfg 打包在 pmpcfg0） |
| 匹配模式 | OFF / TOR（区间） / NAPOT（2 次幂对齐）；**NA4 未实现**（硬连 0） |
| 权限 | R / W / X + **L（锁）** |
| 粒度 | **最小 4KB**（非规范允许的 4B，地址右移 12 位） |
| 最大区 | 1TB（NAPOT） |
| 检查时机 | 各物理访问地址形成后组合检查；5 个通道分别来自 D-uTLB 端口0/1、I-uTLB、PTW PTE 地址和 JTLB/PFU |
| 违例 | 抛 access fault 异常 |

**M 态语义的两个要点**：①无规则匹配时 **M 态默认全通（RWX）**、S/U 态默认全禁——
所以裸机 M 态不配 PMP 也能跑；②**带 L 锁的规则连 M 态自己也受约束**（且锁定后只有复位
能改），这是安全启动里"M 态自缚手脚、防止被攻破后乱改内存"的关键。配合 **MPRV** 位，
load/store 可按 MPP 的特权级查 PMP（M 态代访 U 态内存的场景）。

> 注意 PMP 只管"能不能访问"，"能不能缓存/是否强序"等内存属性由 MMU 的 XMAE/sysmap 管（§8.5）。

**三种匹配模式背后的编码思想——一个寄存器怎么同时编码"基址 + 大小"**：
- **TOR（区间）**：用相邻两项 `pmpaddr[i-1] ≤ addr < pmpaddr[i]` 划区间，大小任意，但要两个
  寄存器配合。
- **NAPOT（自然对齐 2 次幂）——一处精妙的单寄存器编码**：把"基址 + 大小"塞进**一个**地址
  寄存器，靠内部保存值末尾连续 `1` 的个数表示大小：内部末位为 `0`→4KB、
  `…01`→8KB、`…011`→16KB，末尾 k 个 1 对应 `4KB × 2^k`。这里的内部 bit0
  实际来自 CSR `pmpaddr[9]`，更低的 `[8:0]` 已被 4KB 粒度省略，不能套用标准
  4-byte 粒度实现的位号公式。匹配只需一次掩码与比较。
  **用编码技巧把两个量压进一个寄存器、再用一次按位运算判完**——这是体系结构里反复出现的
  方法（同类还有浮点隐含位、cache tag 复用地址位）。
- C910 **不实现 NA4**（4 字节粒度），最小 4KB——4B 粒度的比较器代价高、用得少，砍掉这个
  昂贵极端档是典型取舍。

**为什么"翻译之后查 + 5 个并行检查器"**：PMP 查的是物理地址，必须排在 MMU 翻译之后；而
一拍内可能同时有多笔访存/取指要查（3 访存 + 预取 + PTW），故复制 **5 个并行比较器**让 PMP
不成为流水线瓶颈——**空间换吞吐**的又一例。

### 13.2 PMU / HPCP 硬件性能计数器（RTL: `pmu/rtl/ct_hpcp_*`）

PMU 是性能分析的眼睛（前文 §9.5 的瓶颈定位全靠它）：

| 项目 | C910 |
|------|------|
| 固定计数器 | 2 个：`cycle`（周期）、`instret`（退休指令数），均 64 位 |
| 可编程计数器 | **16 个**（`mhpmcounter3~18`），64 位，各自经 `mhpmevent` 选 1 个事件 |
| 可计事件 | **42 种**（6 bit 事件号） |
| 溢出中断 | 有：`mcntinten`/`mcntof` 控制视图；实际计数主体只到 HPM18，较高控制位不能误解为存在相应计数器 |
| 计数控制 | `mcountinhibit` 逐计数器开关 + 按特权级过滤（PMDM/PMDS/PMDU 分别屏蔽 M/S/U 态计数） |
| 访问 | 标准 M/U 地址与 C910 自定义 S 别名共享底层计数状态；访问是否合法仍由 CP0 授权、委派和特权条件决定 |

**超标量友好——一拍可加多**：计数加法器是 4 bit，`instret` 一拍把 3 个退休槽（每槽含
1~3 条非 split 折叠指令）累加，表达式峰值可一次 +9；`event20` 统计 8 条 RF pipe 的
launch/latch fail 状态之和，可一次 +8，它不是“物理寄存器分配失败”事件。**否则
3 发射核的退休计数会失真**。

**42 个事件里最该知道的几类**（对照 §9.5 调优）：
```
取指/分支:  icache访问/miss、BHT误预测、跳转误预测、conditional/jump 退休数
TLB:        iutlb/dutlb/jtlb miss
访存:       dcache 读/写 访问与 miss、SQ discard、跨4K stall、spec fail（顺序违例）
顶层 stall:  frontend stall、backend stall、sync(fence) stall
指令构成:    IR 级 ALU/load-store/vector/CSR/sync/FPU/ecall 指令数
```

**怎么用 PMU 做性能分析——通用方法（这才是 PMU 的价值）**：
1. **选事件**：给 `mhpmcounter3~18` 各自的 `mhpmevent` 写上要计的事件号；
2. **圈定区间**：清零计数器，用 `mcountinhibit` 精确框住要测的代码段（启停计数），跑目标代码；
3. **读差值、算比率**——**裸计数没意义，派生指标才有**：
   - **IPC** = instret / cycle（越接近 3 越好，§3.2）——总体效率；
   - **分支误预测率** = BHT 误预测 / 分支数 → 前端瓶颈（§4）；
   - **MPKI**（每千条指令 miss）= dcache miss / instret × 1000 → 访存瓶颈（§7）；
   - **前端/后端 stall 占比** = frontend/backend stall / cycle → 是"喂不饱"还是"咽不下"（§9.5）；
4. **采样剖析（profiling）**：把计数器预置到接近回卷并打开相应溢出中断，溢出后在
   中断处理程序记录观察到的 PC，积累近似热点分布。事件先经过增量寄存、64 位计数、
   sticky overflow 和 CP0/RTU 中断入口，且一次增量可大于 1，所以采样 PC 只是事件
   附近的执行位置，不是产生该事件的精确指令 PC。它能支撑统计剖析，但必须结合采样
   偏差、区间边界和中断开销解释。

**为什么超标量计数必须"一拍可加多"**：若加法器只能 +1，3 发射核一拍退休 3 条时 instret 就
少计了。所以 instret 加法器是多 bit、一拍把 3 个退休槽累加——**计数器的累加宽度必须匹配
被计事件的并行度，否则测量本身就失真**。这是一个容易被忽视、却决定 PMU 是否可信的细节。

### 13.3 HAD 调试与跟踪（RTL: `had/rtl/ct_had_*`）

**先理解硬件调试的核心思想——"停核 + 注入指令 + 借指令窥探"**。调试器（GDB 那头）并不能
凭空读核内部的寄存器和内存，它的通用套路是：
1. **halt**：通过 JTAG 让核进入调试态，流水线停在一个干净的边界（像一次特殊的"陷入"）；
2. **注入指令**：调试态下关掉正常取指，改由调试模块往 IFU **喂指令**（`had_ifu_ir`）；
3. **借指令窥探**：想读 `x5`？注入一条把 `x5` 送出去的指令，结果经 WBBR 回读给调试器；
   其它内存访问可由普通注入指令和系统调试路径完成；当前 DDC 只会自动合成
   `addi/addi/sd`，把扫入的数据连续写到内存，没有对称 load/readback 状态；
4. **resume**：看够了恢复现场、跳回原 PC 继续跑。

**所以"调试能力"是用『注入指令 + 观察其结果』间接搭出来的**——这是一切 run-control（JTAG）
调试器的通用原理。RISC-V Debug Spec 的 program buffer / abstract command 本质也是这套，只是
C910 用了 C-SKY 私有的 HACR 寄存器体系来实现。理解了这个套路，下表的每一项就都是它的零件：

C910 用的是**玄铁/C-SKY 自有的 HAD 调试协议**（HAD v2.3），**不是 RISC-V Debug Spec**：

| 维度 | C910 HAD | 对比 RISC-V Debug |
|------|----------|-------------------|
| JTAG | TAP + **16 位 IR（HACR）**，含 bank/index/core 选择 | 标准 5 位 IR + DMI |
| 取数寄存器 | HAD_ID/PC/CSR/WBBR/BABA… 多 bank | dmcontrol/dmstatus/abstractcs |
| 进入调试 | `had_rtu_*_dbgreq`（JTAG break / 硬件断点 / trace） | `haltreq` |
| 调试态执行 | 直接往 IFU 注入指令 `had_ifu_ir` + WBBR 回写 | program buffer |
| 硬件断点 | **2 个**（BABA/BAMA 地址掩码匹配 + RC 取反 + 8 位过 N 次计数 + BC 条件：取指/分支/load/store） | tdata1/2/3 触发器 |
| DDC | 扫入地址/数据，硬件合成 `addi/addi/sd`，每次连续写 8 字节；当前没有自动读回状态 | abstract memory access |
| PCFIFO | **32 项**环形缓冲；IFU 每拍最多两路请求，带链接跳转可展开，因此内部最多写 3 项 | — |
| 跟踪 | OTC 8 位倒计数：写入 N 后先递减 N 次，在第 N+1 个合格退休点请求 debug，且不自动重装；不是包流式 trace | CoreSight ETM |
| 多核 | **ETM 交叉触发总线**：一核进/出调试，广播让其他核一起 halt/resume | haltreq 广播 |

**最值得记的两点**：①**断点带"过 N 次再停"计数器 + 链式触发（SQC）**，能调"第 N 次循环
才断"这类难题；②**多核 halt-all 交叉触发**，一核停则全簇停，方便调多核竞争 bug。

> 这部分是 C910 的"非教科书"工程实现：用成熟的 C-SKY HAD 体系而非新生的 RISC-V Debug Spec，
> 体现了玄铁脱胎于 C-SKY 处理器家族的历史路径。

### 13.4 可测试性设计（DFT）

量产芯片必须可测，C910 RTL 内置标准 DFT 钩子：
- **扫描链（scan）**：`pad_yy_scan_mode` / `scan_en` 进入扫描测试模式。**每个 ICG 时钟
  门控单元都有一根 `pad_yy_icg_scan_en`**——扫描时强制开门钟，否则门控会挡住扫描移位。
  这正是 §12 时钟门控与 DFT 的接口约定（也解释了"为什么每个 ICG 都留一根 scan_en"）。
- **MBIST（存储器内建自测）**：对 I/D-Cache、L2、TLB、各 SRAM 阵列做产测自检（`mbist`/`bist`）。
- Cache/TLB SRAM 可见 MBIST/ECC 宏接口，但当前生成配置的有效边界不同：
  D-Cache ECC 关闭，L2 纠错核为旁路骨架，I-Cache 的若干 error-refill 路径也固定为 0。
  因此只能把这些接口解释为 DFT/可靠性集成预留，不能笼统写成当前 L1/L2 已由 ECC
  完成运行期纠错；实际 SRAM 产测和纠错能力必须结合宏配置、网表和测试报告确认。

> DFT 看似与体系结构无关，却解释了门控、SRAM、ECC 设计里那些"测试用"端口的由来——
> 可测试性是商用 IP 的硬约束。

---

## 14. 端到端数据流走查（把所有单元串起来）

前面分单元讲了"零件"，这一节把它们装成"整机"——用四条典型指令的全程，串起 §3~§13
的每个结构。**读懂这四条走查，就能完整复述这颗处理器如何工作。**

### 走查一：一条 ALU 指令的一生（理想直通路径）

```
PCGEN  查询寄存器式 L0-BTB；普通 BTB 在后续预测级并行确认或补充目标       §4
IF/IP  I-Cache(VIPT,2路) 命中，way-predict 只点亮一路；predecode 标边界    §4/§8.2
IB     打包进 IBUF(32 项)                                               §4.3
ID     3 译码器之一解码                                                  §5
IR     RAT 分配整数 preg（消除 WAR/WAW）；preg 不够则在此 stall           §5.1/§9.5
IS     派进 AIQ0/AIQ1（深 8）；唤醒-选择，源就绪即发射                     §5.2
RF     读 PRF + 前递网络旁路                                            §6.2
EX1    ALU 算出结果，立刻前递（back-to-back 依赖下一拍可发）              §6.2
EX2    写回 PRF；CBUS 报 ROB"已完成"                                    §6.4/§9.2
退休    ROB 按序到它 → 提交，preg 经 PST 进 RETIRE 态                    §9.2/§9.4
```
**一句话**：该示例展示了顺序前端、乱序执行和顺序退休如何衔接；短 ALU 的 EX1
前递可支持背靠背依赖，但是否连续无气泡仍取决于供给、发射、旁路和退休条件。

### 走查二：一次 load miss 的全程（存储层次总动员）

```
load 在 LSIQ(深 12)发射 → AG 算 VA → D-uTLB 翻译得 PA（miss 则 JTLB→PTW，§8.5）
 → DC 查双 tag + 比 SQ 前递（bytes_vld 字节位图，§7.3）
 → DA 判定 L1 miss → 登记 RB(8 项，形成并行 miss 跟踪资源之一)            §7.4
 → BIU read_channel 上 ACE AR（按 AXI ID 跟踪）                         §10.6
 → CIU 查 snoop filter：别核有副本就侦听(可 cache-to-cache)，否则下查 L2  §10.3
 → L2(16 路, 2 bank, tag-cmp-data-wb 主路径) 命中则返数据；miss 再向下层请求 §8.4
 → 关键字优先：要的那个字先经 R 通道交付 ld_wb，load 不等整行            §7.4
 → 整行入 LFB → 写回 D-Cache（状态阵列选择受害路，脏行经 VB 逐出）       §8.3
```
**一句话**：RB 让后续无关访问继续跑（非阻塞），关键字优先把有效延迟压到最短，
BIU/CIU/L2 一路把数据捞回来。

### 走查三：一次分支误预测的恢复（前端抢跑 + 后端精确清理）

```
BJU 在 EX1 发现误预测（方向或 jalr 目标错，对比 PCFIFO 预存值）          §6.2
EX2 三枪齐发：① iu_ifu_chgflw 让 IFU 立刻从正确目标取指（抢回延迟）
            ② cancel 杀 IU 错误路径   ③ mispred_stall 把新指令卡在 ID    §6.2
后端继续跑老指令（错误路径被 cancel 压住，重命名表此刻是污染态）
分支退休 → RTU flush 状态机：FLUSH_IS → 等已退休写回与已提交数据满足条件 → FLUSH_BE
 → 发 rtu_yy_xx_flush：RAT 在该恢复沿装入 PST 导出的 RETIRE 映射；PST、IQ、
   LSU 等接收模块分别按本地 flush/cancel 规则清理或保留状态                 §9.3/§9.4
 → 放行新路径指令
```
**一句话**：前端早重定向抢回取指延迟，后端按序 flush 保证精确状态——快与对兼得。

### 走查四：一次跨核一致性读取

```
core1 对某行发生 load miss → core1 BIU 发 ACE 读请求 → CIU PIU/SNB       §10.6/§10.3
SAB 等待 L2 查询并取得 cp[3:0] presence/filter 元数据
CIU 去掉请求源和未启用 PIU 后，对候选核心发 AC snoop
目标核心经 CR 返回响应；若需提供数据，再经 CD 进入 CIU CTCQ
CIU/L2 完成请求方响应，并按事务结果产生 setcp/clrcp、shared/dirty/pend 更新
```

这条走查能够在波形中直接验证请求、定向侦听、响应、数据和元数据更新的闭环。若要进一步
声称发生了 `M→O`、`I→S` 或数据完全绕过外存，还必须同时确认核内 Cache line 状态、
CRRESP/CD 数据来源、L2 命中与外部读写通道；不能由 `cp` 或 CTCQ 单个信号替代整条证明链。

---

## 15. 核心体系结构权衡总表（对照教科书）

这是本文最该记住的一张表——C910 每个"为什么这么设计"的浓缩：

| 设计点 | C910 的选择 | 取舍 / 动机 | 教科书坐标 |
|--------|-------------|-------------|-----------|
| 执行模型 | 乱序 + 重命名 + 推测 | 挖 ILP，代价是复杂的恢复逻辑 | H&P 3 章 / Tomasulo |
| 重命名方式 | 统一物理寄存器堆 + RAT | 比"ROB 存数据"省数据搬移 | R10000/Alpha 21264 学派 |
| 发射队列 | 7 个分布式队列，AIQ/VIQ 各 8 项，BIQ/LSIQ/SDIQ 各 12 项 | 各类指令独立选择，可能出现某队列满而其他队列仍空 | Shen&Lipasti 分布式 RS |
| 整数 preg 数 | 96 个物理编号，preg0 固定，preg1~95 有状态 | 32 个已退休映射与推测分配共享同一 PST 生命周期 | 统一 PRF + RAT/PST |
| 分支方向预测 | 64 Kbit Predict + 2 Kbit Select 的 Bi-Mode BHT | 将相反偏置分到逻辑两侧，仍保留索引别名 | McFarling/Bi-Mode |
| 目标预测 | 16 项寄存器 L0-BTB + 当前 512 行×4 固定位置槽 BTB | 早期小表与后级容量表协同，错误由 reissue/flush 恢复 | 分级预测 |
| 误预测恢复 | 早重定向前端 + 晚 flush 后端 | 抢回取指延迟 + 保证精确 | 精确异常 |
| ROB | 64 个 40-bit 物理项，每项最多折叠 3 条指令 | PC、结果和异常分别由 PCFIFO/PRF/异常项承接 | 折叠 ROB 与外置状态 |
| 退休 PC | 退休游标结合每项 PC offset 和 PCFIFO 改流信息重建 | 不在每个 ROB 物理项保存完整 PC | 增量状态恢复 |
| 重命名恢复 | PST RETIRE 位图编码后并行写回整数/浮点 RAT | 不 walk ROB；代价是 PST 状态、编码网络和宽恢复总线 | 对比 checkpoint 与 ROB walk |
| I/D Cache 索引 | VIPT / PIPT 分用 | 取指省周期 / 访存避别名 | H&P 2 章 cache 别名 |
| Cache 替换 | I-Cache 两路轮转、D-Cache 状态位、L2 one-hot 轮转 | 普通命中不维护完整最近使用顺序 | 替换策略权衡 |
| 非阻塞 cache | RB 8 项、LFB 8 个地址项 + 2 个 data entry | 支持多个 miss 在途，实际 MLP 还受依赖、TLB 和总线限制 | hit-under-miss |
| miss 交付 | 关键字优先 | 缩短 load 有效延迟 | critical word first |
| store→load | 前递 / 重发，不拼接 | 数据通路简单换偶发重发 | store-to-load forwarding |
| 访存依赖 | IFU 的 PC 相关 SFP + LSU 的 store 地址/字节/IID 短期相关器 | 用后端违例反馈约束后续推测，同时保留精确的地址重叠确认 | memory-dependence prediction / replay |
| 长延迟单元 | DIV/MULT 搭 ALU 管线 | 共享端口省面积 | 资源共享 |
| 浮点异常 | 不 trap，用 fflags | 简化精确异常逻辑 | IEEE 推荐 |
| ISA 扩展 | XIE/XTheadC 复合指令 | 可减少特定代码的动态指令数；收益必须在固定编译和运行条件下测量 | ISA 与编译器协同 |
| 一致性 | 产品资料称 MOESI；当前 RTL 可确认集中事务调度、presence/filter 与定向侦听 | 以压缩元数据和事务状态协作，协议状态需端到端验证 | H&P 5 章 |

---

## 16. 与经典处理器的横向对比

把 C910 放进体系结构史的坐标系，更容易理解它"取了谁、舍了谁"：

| 维度 | C910 | MIPS R10000 (1996, 教科书经典) | Alpha 21264 (1998) | ARM Cortex-A72 (同代对标) |
|------|------|-------------------------------|--------------------|---------------------------|
| ISA | RV64GC+XIE | MIPS IV | Alpha | ARMv8-A |
| 译码宽度 | 3 | 4 | 4 | 3 |
| 乱序窗口(ROB/等价) | 64 个物理项，理想最多折叠约 192 条指令 | 32 | 80 (in-flight) | 公开资料常见约 128，需绑定具体版本 |
| 物理寄存器(整数) | 96 | 64 | 80 | ~128 |
| 重命名 | 统一 PRF + RAT | 统一 PRF + active list | PRF + checkpoint | 统一 PRF |
| 分支方向预测 | Bi-Mode | 2-bit | 局部+全局锦标赛 | 锦标赛 |
| 误预测恢复 | PST 位图重建 | active list 回滚 | checkpoint 回滚 | — |
| L1 I/D | 64K/64K 2路 | 32K/32K 2路 | 64K/64K 2路 | 48K/32K |
| 访存乱序 | LQ16/SQ12 + IFU SFP 反馈 + LSU 地址相关确认 | 顺序 LSU | load 推测 + 重放 | 乱序 LSU |
| 流水深度 | 12 | ~6 | 7 | 15 |

**C910 的定位**：它采用统一 PRF、RAT/PST、分布式发射队列、顺序退休和精确恢复，
与 R10000/21264 一脉的乱序设计有清晰对应；折叠 ROB、退休 PC 重建和共享执行端口则是
当前实现的具体组织方式。与表中后来出现的超宽前端和更大后端资源相比，当前 RTL 的
三路译码、分布式小队列和共享执行端口体现了明确的资源约束；但这些选择是否达成了更好的
频率、面积或能效平衡，仍必须由同工艺、同约束下的 PPA 数据证明，不能仅由 RTL 容量反推。

### 关于物理实现指标（PPA）的说明

> **主频、面积、功耗（PPA）不在 RTL 里，取决于工艺库与后端实现，需查官方数据手册。**
> 除明确标注为论文、手册或产品口径的数据外，本文的结构性数字来自当前仓库 RTL；
> PPA 属实现层，同一份 RTL 在不同工艺、库和约束下会得到不同的主频、面积与功耗。
> 可公开参考的产品量级为 C910 在 **12nm 级工艺约 2.0~2.5 GHz**；
> 本仓库历史 CoreMark 配置之间的分数差异不能单独量化 ISA 扩展贡献。**精确 PPA 请以目标工艺的综合/后端报告或
> T-Head 官方 datasheet 为准**，不要把本文的结构参数当成 PPA 指标。

---

## 17. 全参数速查表

```
═══════════════════════════════════════════════════════════════════════
 顶层
   ISA            RV64GC + XIE/XTheadC + XMAE, M/S/U；当前 misa.V=0
   流水线          官方产品口径 12 级整数, 3 译码 / 8 执行管线 / 3 ROB项退休
   多核            当前顶层 core0/core1 有效, CIU/L2 保留 4 参与者编码空间
   总线            核侧 128-bit ACE 通道，经 CIU/L2 接入下层系统
═══════════════════════════════════════════════════════════════════════
 IFU 取指 + 分支预测
   I-Cache        64KB / 2 路 / 64B 行 / VIPT / 两路轮转 / Predecode 阵列
   L0-BTB         16 项寄存器快速表
   BTB            当前生成RTL为512物理行×4固定位置槽；10-bit逻辑index的bit9未接入SRAM地址
   BHT            Bi-Mode, 64Kbit Predict + 2Kbit Select, 22-bit GHR 分段参与索引
   RAS            12 项投机栈 + 6 项退休恢复副本（预测深度不是18）
   IBUF           32 项 (每项 1 个 16 位 half-word, RVC 边界对齐), 出≤3/拍, 支持 bypass
═══════════════════════════════════════════════════════════════════════
 IDU 译码/重命名/发射
   译码            3 路并行
   派遣            ≤4 微操作/拍
   重命名表        整数RAT / 浮点RAT 有效；向量VRT为当前dummy边界
   物理寄存器      整数96 / 浮点64 / ereg；向量物理状态不可按完整路径使用
   发射队列        7 个 (总 68 项): AIQ0/AIQ1=8, BIQ/LSIQ/SDIQ=12, VIQ0/VIQ1=8
═══════════════════════════════════════════════════════════════════════
 执行单元 (8 管线)
   pipe0          ALU0 + DIV(SRT16变延迟, 2项结果缓存) + SPECIAL(CSR/vsetvl)
   pipe1          ALU1 + MULT(4拍, 65×65, MLA私有前递)
   pipe2          BJU (误预测早重定向+晚清理, PCFIFO 32项)
   pipe3/4/5      LSU load / store-addr / store-data
   pipe6/7        VFPU0/1 (当前标量F/D；保留向量接口；FMA最长到EX5)
   ALU 延迟       短指令 EX1 前递(1拍) / 长指令 EX2 写回(2拍)
═══════════════════════════════════════════════════════════════════════
 LSU 访存
   流水            4 级 AG/DC/DA/WB
   LQ / SQ / WMB  16 / 12 / 8 项 (LQ违例, SQ推测域, WMB提交域)
   RB / LFB / VB  8 / (addr8+data2) / 2 项   →  提供多 miss 跟踪与关键字优先；实际 MLP 受全链路约束
   PFU            PMB→SDB→PFB(8 流), L1/L2 双状态机
   SNQ            6 项 (一致性侦听)
   依赖推测        bytes_vld[15:0] 字节重叠；IFU SFP 按PC保存预测状态，LSU短期相关器保存地址/IID
═══════════════════════════════════════════════════════════════════════
 存储管理
   I-Cache        64KB / 2 路 / 512 组 / 64B / VIPT / 两路轮转 / predecode / way-predict / ECC 宏分支
   D-Cache        64KB / 2 路 / 512 组 / 64B / PIPT / 双tag(54+52b) / 全cache 8 bank×32b / dirty阵列512×7 / cache_buffer / 当前ECC关闭
   L2-Cache       1MB / 16 路 / 1024 组 / 64B / PIPT / 2 sub-bank / tag-cmp-data-wb主路径 / one-hot轮转 / ECC框架旁路 / 可编程延迟 / I+TLB预取 / 响应rfifo(3项×14位)
   MMU            Sv39单阶段; I-uTLB 32(4 fast slot, PLRU) / D-uTLB 16普通+1个1GB项(PLRU); JTLB 1024=256×4(one-hot轮转, ASID16b+global); PTW 3级走LSU; MAEE显式选择PTE XMAE或8区sysmap
═══════════════════════════════════════════════════════════════════════
 RTU 退休
   IID            7 位 = 1 wrap + 6 ROB 索引
   ROB            64 个物理项 × 40 位, 折叠≤3条/项, 退休3项/拍(峰值9条、持续吞吐仍受3路译码约束)
   异常缓冲        单项擂台 (10 compare_iid), 误预测/违例/vsetvl 复用
   flush          IDLE→FLUSH_FE/IS→WF_EMPTY→FLUSH_BE→IDLE; rtu_yy_xx_flush
   PST            preg96/freg64/ereg有状态；vreg PST为dummy；退休态位图重建整数/浮点RAT
═══════════════════════════════════════════════════════════════════════
 中断/异常
   特权级          M/S/U; trap CSR 全套; mtvec Direct/Vectored; medeleg/mideleg 委派; 无 NMI/CLIC
   CLINT          2 hart; mtime/mtimecmp 64b; msip 软中断; 另供 S 态 stimecmp
   PLIC           144外部源 + 16内部位置(含保留/L2 ECC入口); 当前2 hart; 5b优先级; M/S路径; claim/complete; APB
 低功耗
   时钟门控        gated_clk_cell 使用 global/module/local/scan 等使能；行为模型是否真正停钟取决于编译宏
   硬件省功耗      way-predict / per-bank激活 / Loop Buffer / 变延迟乘除(skip_srt) / BIU 通道门控+no_op
   软件可关        mhcr: ie/de/rse/bpe/btbe/ibpe/l0btbe;  mhint: I/D/L2预取·way-predict(iwpe)·loop(lpe)·ECC
   WFI            lpmd 3 态 IDLE→WAIT(等IFU/LSU/BIU/MMU no_op)→LPMD; forever时钟域观察中断/调试唤醒
   DFT            scan/MBIST接口与ECC宏分支可见；当前L2开源ECC纠错核未落地
 保护/可观测/调试
   PMP            8 区; OFF/TOR/NAPOT (NA4 未实现); R/W/X+L; 最小 4KB; 翻译后查物理地址
   PMU/HPCP       2 固定(cycle/instret)+16 可编程计数器(64b); 42 事件; 溢出中断; 按特权过滤; 超标量一拍可加多
   HAD 调试        玄铁 C-SKY HAD v2.3 (非 RISC-V Debug); 16b HACR IR; 2 硬件断点(地址掩码+过N次); DDC; IU PCFIFO 32项; 多核 halt-all
   其他            CP0 控制寄存器 · BIU/AXI 总线接口
═══════════════════════════════════════════════════════════════════════
```

---

## 18. 术语与缩略语速查表

按子系统分组，方便边读边查（每条标注首次详述的小节）：

**总体 / 流水线**
| 缩写 | 全称 | 含义 | 小节 |
|------|------|------|------|
| ILP | Instruction-Level Parallelism | 指令级并行 | §1 |
| IPC | Instructions Per Cycle | 每周期退休指令数 | §3.2 |
| OoO | Out-of-Order | 乱序执行 | §3 |
| IID | Instruction ID | 贯穿全核的指令身份证（7 bit） | §9.1 |

**前端 IFU**
| 缩写 | 全称 | 含义 | 小节 |
|------|------|------|------|
| BTB | Branch Target Buffer | 当前生成 RTL 为 512 行×4 个固定位置槽 | §4.2 |
| L0-BTB | Level-0 BTB | 16 项低延迟寄存器目标表，命中向量先打拍 | §4.2 |
| BHT | Branch History Table | 条件分支方向表（Bi-Mode，64Kb） | §4.2 |
| RAS | Return Address Stack | 返回地址栈（12 项投机栈 + 6 项退休恢复副本） | §4.2 |
| GHR | Global History Register | 22-bit 全局历史；不同预测路径选择窗口并分段参与索引 | §4.2 |
| VIPT/PIPT | Virtually/Physically Indexed Physically Tagged | 虚/物理索引物理标签 | §8.1 |
| IBUF | Instruction Buffer | 取指缓冲（32 项） | §4.3 |

**乱序引擎 IDU / RTU**
| 缩写 | 全称 | 含义 | 小节 |
|------|------|------|------|
| RAT | Register Alias Table | 重命名表（整/浮/向各一） | §5.1 |
| PRF | Physical Register File | 当前有效物理寄存器堆为整数 96 项、标量浮点 64 项；向量重命名路径为 dummy 边界 | §5.1 |
| preg/vreg/ereg | physical reg | 整数/标量浮点接口沿用的 `vreg`/向量配置接口沿用的 `ereg` 物理编号；后两者必须结合当前 `misa.V=0` 理解 | §5.1/§6.6 |
| AIQ/BIQ/LSIQ/SDIQ/VIQ | Issue Queues | 7 个分布式发射队列（8~12 项） | §5.2 |
| ROB | ReOrder Buffer | 重排序缓冲（64 项×40 bit） | §9.2 |
| PST | Physical-register Status Table | 物理寄存器生命周期表（5 态） | §9.4 |
| PCFIFO | PC FIFO | 跳转 PC 暂存（32 项，省 ROB 面积） | §6.2 |

**执行单元 IU / VFPU**
| 缩写 | 全称 | 含义 | 小节 |
|------|------|------|------|
| ALU | Arithmetic Logic Unit | 两条整数 ALU 管线；能否同拍使用取决于队列、依赖和端口约束 | §6.2 |
| BJU | Branch Jump Unit | 分支跳转单元 | §6.2 |
| MULT/DIV | Multiplier/Divider | 乘法器(4拍)/除法器(SRT-16) | §6.2 |
| vfalu/vfmau/vfdsu | Vector FP ALU/MAU/DSU | 浮点加比转/乘加/除开方 子单元 | §6.3 |
| FMA | Fused Multiply-Add | 融合乘加（5 拍） | §6.3 |
| CBUS/RBUS | Complete/Result Bus | 完成总线/结果总线 | §6.4 |
| MTVR | Move To Vector Reg | 保留的整数→向量寄存器接口；当前生成配置不构成可执行 RVV 数据通路 | §6.3 |
| VLEN/SEW/LMUL | Vector Length/Element Width/Length Mul | 保留的向量长度、元素宽和寄存器组倍数控制接口；当前 `misa.V=0` | §6.6 |

**访存 LSU / 存储层次**
| 缩写 | 全称 | 含义 | 小节 |
|------|------|------|------|
| LQ/SQ | Load/Store Queue | 加载队列(16)/存储队列(12) | §7.2 |
| WMB | Write Merge Buffer | 写合并缓冲（8，已提交域） | §7.2 |
| RB | miss Request Buffer | miss 请求缓冲（8，定 MLP） | §7.2 |
| LFB | Line Fill Buffer | 行填充缓冲（addr8/data2） | §7.2 |
| VB | Victim Buffer | 逐出缓冲（2） | §7.2 |
| SNQ | Snoop Queue | 侦听队列（6） | §7.2 |
| VMB | Vector Memory Buffer | LSU 中保留的 8 项向量访存缓冲结构；当前向量 ISA 路径未启用 | §6.6 |
| PFU | PreFetch Unit | 硬件预取 | §7.4 |
| MLP | Memory-Level Parallelism | 内存级并行（≈8） | §7.4 |
| RVWMO | RISC-V Weak Memory Ordering | RISC-V 弱内存序 | §7.5 |
| LM | Local Monitor | LR/SC 本地监视器 | §7.5 |
| TLB/uTLB/JTLB | (micro/Joint) Translation Lookaside Buffer | 地址翻译缓冲 | §8.5 |
| PTW | Page Table Walker | 硬件页表遍历 | §8.5 |
| ASID | Address Space ID | 地址空间标识（16 bit） | §8.5 |
| XMAE | XuanTie Memory Attribute Extension | 玄铁内存属性扩展 | §8.5 |

**系统 / 多核 / 外围**
| 缩写 | 全称 | 含义 | 小节 |
|------|------|------|------|
| BIU | Bus Interface Unit | 每核 AXI/ACE 主端口 | §10.6 |
| CIU | Coherence Interconnect Unit | 一致性互联 + 侦听过滤 | §10.3 |
| MOESI | Modified/Owned/Exclusive/Shared/Invalid | 产品资料使用的五态一致性模型；当前 RTL 状态需按事务链验证 | §10.2 |
| ACE | AXI Coherency Extensions | AXI 一致性扩展 | §10.4 |
| CTC | Cache-To-Cache transfer | 核间直接传脏行 | §10.3 |
| SNB | Snoop Broadcast | 侦听广播 | §10.3 |
| CLINT | Core-Local Interruptor | 核内定时器/软中断 | §11.2 |
| PLIC | Platform-Level Interrupt Controller | 144 路外部中断输入映射到 160 位内部源总线；当前实现 2 hart | §11.3 |
| PMP | Physical Memory Protection | 物理内存保护（8 区） | §13.1 |
| PMU/HPCP | Performance Monitor / HW Perf Counter | 性能计数器（2+16，42 事件） | §13.2 |
| HAD | (XuanTie) Hardware Aided Debug | 玄铁硬件调试 | §13.3 |
| ICG | Integrated Clock Gating | 集成时钟门控单元 | §12.1 |
| WFI/LPMD | Wait For Interrupt / Low-Power Mode | 等中断休眠 | §12.3 |

---

## 学习路径建议

1. **先看本文 + 框图**，建立全局骨架与"为什么"的直觉；
2. 按兴趣深入 [IFU](./ifu/README.md)、[IDU](./idu/README.md)、
   [IU](./iu/README.md)、[LSU](./lsu/README.md) 和 [RTU](./rtu/README.md)
   （每个目录有 `00_*_overview.md` 总览、编号子文档和 `README.md` 索引）；
3. 跑 `smart_run` 的 bench（coremark / bench_frontend / bench_mem）配合 Verdi 看波形，
   各单元总览末尾都列了"Verdi 观察层次"与入门信号组；
4. 对照本文第 15、16 节的权衡表反复回味——**记住设计动机比记住数字更重要**；
   并用第 14 节的"端到端走查"把各单元串成一条指令的全程。

---

*文档基于 OpenC910 开源代码（Apache-2.0）与官方数据手册整理。*
*配套框图：`C910_架构框图.png` / `.svg`（源 `C910_架构框图.dot`，graphviz 渲染）。*
